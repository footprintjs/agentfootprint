---
title: formatArtifactStoreReport
---

# Function: formatArtifactStoreReport()

> **formatArtifactStoreReport**(`report`): `string`

Defined in: [src/artifacts/conformance/run.ts:222](https://github.com/footprintjs/agentfootprint/blob/main/src/artifacts/conformance/run.ts#L222)

The report as something to put in a failure message or a log — one line per
case, with the reason a case did not simply pass.

## Parameters

### report

[`ArtifactStoreReport`](/docs/api/interfaces/ArtifactStoreReport)

## Returns

`string`
