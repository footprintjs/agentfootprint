---
title: projectWalk
---

# Function: projectWalk()

> **projectWalk**(`entries`, `cap`): [`ProjectedWalk`](/docs/api/interfaces/ProjectedWalk)

Defined in: [src/core/runbook/walk.ts:78](https://github.com/footprintjs/agentfootprint/blob/main/src/core/runbook/walk.ts#L78)

Apply the cap law. Counters are about the WHOLE narrative (`total`), so a
 projected walk cannot read as a short run.

## Parameters

### entries

readonly `NarrativeEntryView`[]

### cap

`number`

## Returns

[`ProjectedWalk`](/docs/api/interfaces/ProjectedWalk)
