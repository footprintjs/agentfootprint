# Findings ledger — worklog

One line per task, newest last. Each task: built → Opus review → Sonnet
review → fixes → bench → commit. Read before touching any step; the design
page (`2026-09-findings-ledger.md`) holds the plan, this holds what happened.

- 2026-09-16 · step 1 · bench `bench/findings-context.mjs` + baseline (no window 6/6 facts, noise 93.5%; sliding keep-6 2/6 facts). Lesson: run benches over the CJS dist on node, not tsx.
- 2026-09-16 · design refined from the owner's transcript: basis before the call (direct/exploratory + usefulness bucket + sought), ruled-out disposition, ledger-only answer turn, shuffle test, instruction as versioned artifact; prior art recorded (SLEUTH 2607.12267, 2606.04990).
- 2026-09-16 · fact · `contextfootprint` 0.1.1 is on npm and already an agentfootprint dependency (`src/integrity/assertion/`); the cross-line question is closed. Understand+design workflow launched (5 seam readers → 3 designs → judge/synthesis).
- 2026-09-16 · spec · understand+design workflow done (9 agents, ~1.7M tokens): winner 'ride-along findings' (a reserved `_findings` argument on the next tool call + a reserved answer key; zero extra calls), corrected with the record-first honesty laws; filed as `2026-09-findings-ledger-spec.md` (line numbers there are the readers' — cite symbols in code and notes).
- 2026-09-16 · step2 · FACT · the spec's "seven adapter tests" are NINE wires on this tree: `FoundryProvider` has no mapping of its own (it composes `openai()`, so its bytes are `OpenAIProvider · toOpenAITool`'s), `FoundryLocalProvider · toFoundryTool` is a separate fetch wire, and "Browser" is two adapters (`BrowserAnthropicProvider · toAnthropicTool`, `BrowserOpenAIProvider · toOpenAITool`; `BrowserAzureOpenAIProvider` composes the latter). Every mapping function is module-private, so `test/adapters/reservedArgumentSurvives.test.ts` reaches each through its public factory with an injected `_client` (Anthropic, OpenAI, Gemini, Bedrock, Foundry) or `_fetch` (Ollama, FoundryLocal, both Browser) — the `forced-tool-choice.test.ts` precedent — and pins the list with a drift guard over `src/adapters/llm` (a new `{ ...schema.inputSchema }` spread must add a wire test).
- 2026-09-16 · step2 · FACT · no adapter rewrites a tool schema (no `strict`, no `additionalProperties` injection): each maps by a shallow `{ ...schema.inputSchema }`, so on the SDK wires `_findings` rides by REFERENCE into the client call; the test deep-freezes the served fixture so an in-place edit throws, and reads bytes through `JSON.stringify` on every wire. The test file lives at `test/adapters/reservedArgumentSurvives.test.ts` (the sibling adapter unit tests are one level down in `test/adapters/unit/`); `tsconfig.json` excludes `test/`, so it was typechecked with a scratchpad tsconfig (bundler resolution + the repo's `node_modules/@types`) — the repo's `test:types` script covers `test/type-regressions/` only.
- 2026-09-16 · step2 · FACT · the `continueFrom` re-seed is NOT one site: `folded` rides `Agent.ts · pendingResumeFolded` (field) → `applyContinuation` (stash) → the seed-deps accessor `consumePendingResumeFolded` (read-and-clear, threaded where seed deps are built) → `seed.ts · consumePendingResumeFolded` (deps type) + the `scope.foldedSpans = [...resumeFolded]` write, and BOTH carriers read it through `Agent.ts · foldedSpansOf` (`checkpoint()` and the crash-carrier `buildCheckpoint` call). `runCheckpoint.ts` is complete (type, `buildCheckpoint` trailing `findingsLedger?` param spread value-conditionally, `validateCheckpoint` present-only check); the seven Agent.ts/seed.ts sites are owned by the Door and Schema stages and are listed in the step-2 events/state result, not edited here.
- 2026-09-16 · step2 · FACT · the re-seed in `seed.ts` is a RESTORE of a stored record (the `foldedSpans` precedent: `if (ledger && ledger.length > 0) scope.findingsLedger = [...ledger]`), never a `recordFindings` call — it files no row and emits no event, so the "one writer" law and the "re-seed only when the checkpoint carries it" law both hold.
- 2026-09-16 · step2 · FACT · `test/events/unit/registry.test.ts` pins EIGHT docs to "<N> typed events … <D> domains" (CLAUDE.md, AGENTS.md, docs/MENTAL_MODEL.md, ai-instructions/claude-code/SKILL.md, docs-next debug/debug.mdx, reference/dependency-graph.mdx, monitor/observability.mdx, build/agent.mdx); `findings.{declared,standing}` moves them from 111/24 to 113/25 — the docs stage owns the sentence edits; the count pin in the test is already bumped to 113.
- 2026-09-16 · step2 · FACT · payload types reach the public surface through `src/events.ts · export type * as Payloads` — a star export, so `FindingsDeclaredPayload` / `FindingsStandingPayload` need no hand-listed barrel row.
- 2026-09-16 · step2 · FACT · `scripts/docs-truth-check.mjs` enumerates `ALL_EVENT_TYPES` and ratchets against `docs/docs-truth/baseline.json`: a registered event with no site-prose mention is a "new undocumented event" regression — the docs stage adds one sentence naming each event under docs-next (or consciously re-baselines).
- 2026-09-16 · step2 · FACT · `validateCheckpoint` checks `findingsLedger` only when PRESENT (array of plain rows with `kind` ∈ basis | standing | conflict), by the `skillCursor` / `evidenceRecovery` precedent — the spec asked only for the value-conditional carry; the door check is there because the continued run re-seeds the array verbatim.
- 2026-09-16 · step2 · FACT · the spec's `FindingsDeclaredPayload.malformed` has no source in the spec's `BasisRow` (`{kind, toolCallId, toolName, iteration, basis, expect?}`) and `recordFindings(scope, rows)` emits from rows alone (the `middleware/ledger.ts · recordDecisions` precedent), so `BasisRow` carries an optional `malformed` count (present only when non-zero) and `basisRowFrom(tc, declaration, iteration, malformed?)` takes it as a trailing optional — additive; the dispatch stage hands it `splitFindings(tc.args).malformed`. A standing-only declaration's dropped entries have no event to ride and stay a `splitFindings` return value.
- 2026-09-16 · step2 · FACT · `basisRowFrom` REFUSES a declaration without `basis` (a `TypeError` naming the tool) rather than defaulting one — the spec's dispatch site calls it only under `findings?.basis !== undefined`, so the throw is a contract guard the loop can never reach; "never infer a basis" is code, not a comment.
- 2026-09-16 · step2 · FACT · `contextfootprint · conflictsOf` compares only rows with `stratum === 'asserted'` whose value `participates` (a `Claim` of kind unknown / not-applicable never compares), so the stratum rule (open / ruled-out → `'quoted'`) is the whole reason quoted readings stay out of the algebra — `foldLedger` adds no filter of its own. Its `Conflict` is `{ key, assertions }`; `ledger.ts · witnessesOf` resolves witnesses by `assertionKey` over the CURRENT fact rows rather than by reference, because an element read off a TypedScope array is not reference-stable and a key is.
- 2026-09-16 · step2 · FACT · fast-check is not a devDependency (package.json, node_modules), so `test/core/agent/findings/reserved.test.ts` runs its property sections over a seeded mulberry32 generator of `fc.jsonValue()`-shaped values (200 seeds; the seed is in every failure message). `test/helpers/modelFacingClaims.ts` exports no `tool-description` surface other than `GRAPH_TOOL_DESCRIPTION` (request-ephemeral) and no persistent `system-text` one; `unprovable` judges on `lifetime` alone, so the test builds local `'persistent-history'` surfaces for both channels — the strictest gate — instead of editing a helper this stage does not own; the registration stage still names each string's real lifetime in its PRODUCERS rows.
- 2026-09-16 · step2 · FACT · `test/core/agent/findings/ledger.test.ts` runs `recordFindings` inside a real footprintjs `flowChart` with two stages (a `ScopeRecorder` with only `onEmit` captures the events): the second stage's read of `findingsLedger` is the live proxy view, and the committed key after both stages is structuredClone-safe with the conflict row appended last — the `recordDecisions` spread-first pattern holds for rows that nest assertions. `ArtifactMeta` requires `createdAt`, so the placed-ticket fixture is minted through `artifacts/placement.ts · placedToolResult` with a full meta, never hand-written.
- 2026-09-16 · step2 · FACT · the pure module compiles against the parallel-landed registry and state as-is (`EVENT_NAMES.findings.{declared,standing}` in `src/events/registry.ts`, `AgentState.findingsLedger` in `src/core/agent/types.ts`): `npx tsc --noEmit -p tsconfig.json` is green on the whole `src/` tree with the four findings files present; `tsconfig.json` excludes `test/`, so the two tests were typechecked through a scratchpad tsconfig (the adapter stage's recipe).
- 2026-09-16 · step2 · FACT · review finding 1 (checkpoint door) applied: `runCheckpoint.ts · validateCheckpoint` now asks `ledgerRowIsWellFormed`, which checks the fields the fold consumes PER KIND (basis: toolCallId/toolName/iteration/basis∈BASIS_VALUES/expect?; standing: toolCallId/standing∈STANDING_VALUES/assertions[]/iteration/declaredOn 'answer'|{toolCallId}; conflict: key/witnesses[]/iteration) — shape only, never the values; P5 carries the review's `{kind:'standing', standing:'fact'}` case plus eight more well-kinded-but-corrupt rows and one well-formed row per kind that must pass.
- 2026-09-16 · step2 · FACT · review finding 2 (docs gates) carried with the wiring: the eight "<N> typed events … <D> domains" sentences read 113/25, `docs-next/content/docs/monitor/observability.mdx` names `agentfootprint.findings.declared` and `.standing` in prose (the docs-truth `EVENT_RE` scan counts a site-prose mention, so the undocumented-events count stays at the baseline), and `npm run docs:truth:report` was regenerated LAST after the final code change.
- 2026-09-16 · step2 · FACT · the spec's "BOTH `permissionChecker.check` sites read `tc.args`" is one site on this tree: the tool-call gate (`context: tc.args`) did; the `askCapability` gate already read `callArgs`. One edit, and `callArgs` is seeded from the peeled `args`.
- 2026-09-16 · step2 · FACT · `buildToolsSlot`'s parameter is `config`, not `deps` — the gate is spelled `config.findings === true`; `merged` itself stays undecorated because the only later read is names-only (`servedNames`), so the committed list is the ONE decorated value.
- 2026-09-16 · step2 · FACT · a `$emit` reaches `agent.on()` only through a per-domain `EmitBridge` (`recorders/core/EmitBridge.ts`, prefix-matched); the spec named no such site. `Agent.buildChart` attaches an `agentfootprint.findings-bridge` (prefix `agentfootprint.findings.`) under the arm, the `middleware-bridge` precedent — an unarmed agent gains no bridge, no listener, no per-event work. Without it the rows landed and no event was delivered (found by the integration test, 4/18 red).
- 2026-09-16 · step2 · FACT · `Agent` cannot carry both a private field `findings` and the public accessor `findings()`; the field is `findingsOptions`. `AgentBuilder.opts` is readonly, so the door stores `findingsValue` and `build()` composes it into the options it hands `Agent` (the `maxIterationsOverride` precedent — an agent with neither override hands its options by reference). An option-object `findings` (`Agent.create({ findings })`) is routed through `.findings()` by the builder constructor so the instruction piece always rides with the arm; a later `.findings()` is then the refused second call.
- 2026-09-16 · step2 · FACT · the route peel lands after the output-chain block and BEFORE the `denied` early return (the standings are about PREVIOUS results and are a fact about the emission, so a withheld answer still files them — the same law as a denied tool call keeping its basis row), and the key is taken off whenever it was present, rows filed only when `previous` was declared. `brokenByChain` judges `preChainAnswer` PEELED under the arm — it is judged, never recorded — or a valid armed answer under a strict schema would read "never valid" and the chain's mistake would be charged to the model again (the 8.18.0 defect, reintroduced for armed agents).
- 2026-09-16 · step2 · FACT · the `continueFrom` re-seed, seven sites as the events/state stage listed: `Agent.pendingResumeFindingsLedger` (stashed UNCONDITIONALLY by `applyContinuation`, like `folded` — honoured only under the arm; cleared in the run's `finally`), `consumePendingResumeFindingsLedger` on the seed deps under the arm, `seed.ts` restores a copy (no `recordFindings`, no event — proven: the continued run fires no findings event), `findingsLedgerOf` = the `foldedSpansOf` twin read by BOTH carriers (`checkpoint()` and the crash `buildCheckpoint` call).
- 2026-09-16 · step2 · FACT · the armed-but-silent twin differs from the unarmed agent by exactly the `agentfootprint.context.injected` events of the `findings-ledger` piece (one per call, `sourceId: 'findings-ledger'`) — the arm is that one visible thing; the committed key set and every other event are equal, and no findings event fires. The integration test pins it that way rather than as a bare event-sequence equality.
- 2026-09-16 · step2 · FACT · `test/core/agent/tool-args-validation.test.ts` is a pure-validator unit file (no agent loop) and `pause-resume.test.ts`'s `pausingAgent` is a describe-local closure, so the spec's two extensions (`additionalProperties:false` under `'enforce'` accepting `_findings`; the pre-8.13 fallback never re-injecting the key) live as self-contained scenarios in `test/core/agent/findings-ledger.test.ts` (18 scenarios: schemas, peel, basis-before-tool_start, standings, unknown id, silent twin, unarmed pass-through, answer turn + `runTyped`, pause/resume, checkpoint/continueFrom, the door, the registry refusal). The 16 byte-identity references passed untouched before and after the wiring.
- 2026-09-16 · step2 · FACT · `src/index.ts` exports `RESERVED_ARGUMENT` and the row types (`FindingsLedger`, `FindingsRow`, `BasisRow`, `StandingRow`, `ConflictRow`, `ConflictWitness`, `FindingsDeclaration`, `Basis`, `Expect`, `Standing`) beside the output-schema block; the door needs nothing extra — `AgentBuilder` was already exported and `.findings()` is a method on it.
- 2026-09-16 · step2 · FACT · two gates the spec did not list surfaced on the whole suite: `test/api-conformance/silent-success.test.ts` classifies EVERY public builder method (`findings` joined `REFUSES_A_SECOND_CALL` with its twice-probe; the refusal names its door), and `test/agent-skill-ships.test.ts` requires `.claude/skills/agentfootprint/SKILL.md` to be byte-identical to the shipped `ai-instructions/claude-code/SKILL.md` (re-installed by `cp` after the 113/25 count edit, the test's own instruction). Neither is a `tmp/` file; both are tracked.
- 2026-09-16 · step2 · byte identity, bench, docs · the 15 references were run on the wired tree BEFORE the scenario existed (16/16 green, the reference directory untouched by git); ONE scenario `agent-findings` added to `SCENARIOS` (a basis on two calls, the first result's standing on the second, a JSON answer carrying the second's standing under a strict output-schema parser) and generated alone (`-t agent-findings` under `AF_TOOLS_REFERENCE=update`; the 15 copied aside first and byte-compared after: none moved); the whole file is 17/17. `npm run bench:findings` now runs each window unarmed and armed and exits non-zero if a baseline column moves: printed none 6/6/14/93.5%/20/41 and sliding 6/2/4/92.3%/6/13 on both rows, calls-with-a-basis 20, results-with-a-standing 19, conflict-rows 0, extra-output-tokens 0, declared-chars 2562. Design page: Step 2 section (name + nine wires, the `standing` ruling, the stratum rule, the last-batch law, the refused synthetic tool, the provider assumptions, the table, the reference), Track ticked, status updated; CHANGELOG 9.101.0 dated 2026-09-16; findings README gains the wire proof + bench facts; top-level README gains one capability bullet.
- 2026-09-16 · step2 · FACT · `MockProvider · buildResponse` estimates `usage.output` as content chars / 4 and never counts tool-call args, which is where `_findings` rides — so the bench's `extra-output-tokens` column (`totalOutputTokens` armed minus unarmed) is 0 by construction of the mock, not of the feature; the bench prints `declared-chars` (JSON chars of the scripted `_findings` values) beside it as a count of the emission's bytes, and the token price of the ask stays a real-model number.
- 2026-09-16 · step2 · FACT · the armed reference moves against its unarmed twin in 104 paths, and two families were not in the spec's byte-identity paragraph: the receipt's `requestMeasurement` sizes (the decorated tools slot, the piece, the args) and the piece's rows on `activeInjections` / `activeByslot` / `systemPromptInjections` — both the arm's own footprint, not a leak; the ledger's second and later writes are recorded with the engine's `append` verb (a fresh array extending the previous one), the first with `set`, and `agent.findings()` returns the four rows the fold expects.
- 2026-09-16 · step2 · FACT · the design page's design/laws/cut/open-questions sections predated the `standing` ruling and said "disposition" for the model's claim at eight sites; aligned to `standing` on this pass (the word stays `integrity/disposition`'s), recorded on the page under "The `standing` ruling".
- 2026-09-16 · step2 · FACT · `scripts/docs-truth-check.mjs` reads `docs-next/content/docs` as the truth source and treats `docs/**` and `README.md` as repo-internal prose, so this stage's design-page, CHANGELOG and README edits do not touch the gate; the "described but never observed firing" count for the two findings events changes only when `npm run docs:truth:exercise` is re-run (it runs `examples/*`, not the test suite, so the new reference scenario does not feed it — an example that arms `.findings()` would).
- 2026-09-16 · step2 · FACT · `test/core/tools/byte-identity.test.ts` has four pre-existing type errors that no config ever checked (vitest transpiles without typechecking): `AgentRunResult` is not exported from `src/index.ts`, the harness's `args: object` is narrower than `LLMResponse.toolCalls[].args`, the `message-api-chart` scenario passes a `tools` key `MessageApiChartDeps` does not declare, and `import.meta.url` is refused under the root `tsconfig.json`'s `module: commonjs` — all four outside this stage's hunks (the header and the `agent-findings` scenario), which add no error under a scratch tsconfig that includes the file beside `src/**`.
- 2026-09-16 · step2 · FACT · two release-gate reds on the handed-over tree, both outside this stage's files: (1) `npm run docs:truth` ratchets RED on 11 NEW undocumented exports — `RESERVED_ARGUMENT`, `Basis`, `Expect`, `Standing`, `FindingsDeclaration`, `FindingsLedger`, `FindingsRow`, `BasisRow`, `StandingRow`, `ConflictRow`, `ConflictWitness` (exported from the root by the wiring stage after the report was last regenerated: the committed `docs/DOCS_TRUTH_REPORT.md` says 2148 exports, the generator now says 2159, so `test/docs/release-gate-interlock.test.ts` fails on the byte comparison too); the fix is one prose sentence naming them on a docs-next page (`build/agent.mdx` is where `.findings()` would be described) or a conscious `npm run docs:truth:baseline`, then `npm run docs:truth:report` run LAST — after every docs/ and README edit, this stage's included. (2) `npm run format` lists five wiring-stage test files unformatted (`test/core/agent-middleware.test.ts`, `test/core/agent/findings-ledger.test.ts`, `test/core/agent/tool-args-validation.test.ts`, `test/core/scenario/pause-resume.test.ts`, `test/lib/time-travel/receipt-conformance.test.ts`); `npx prettier --config .prettierrc.js --write` on those five is the whole fix. This stage's own test file is prettier- and eslint-clean.
- 2026-09-16 · step2 · FACT · the ANSWER emission is on the record PEELED, everywhere: `route.ts · buildEnforcingDecider` overwrites `scope.llmLatestContent` with the peeled JSON whenever the key was present, `outputRetry.ts · buildOutputRetryStage` reads THAT value for the failed-answer turn it writes into `history` (so the corrective turn quotes the peeled failure, not the spec's "RAW"), `prepareFinal` runs in a non-merging subflow (the final answer is not in `sharedState.history` at all) and `Agent.checkpoint()` appends `lastRunAnswer` — the peeled answer — as the conversation carrier's assistant turn. Only the TOOL-CALL emission (`toolCalls[].args`, `llmLatestToolCalls`) stays raw. Pinned as the code's shape in `test/core/agent/findings-ledger.test.ts` (§8 answer turn, §11 retry) with the drift named; not changed here (route/outputRetry are not this stage's files) — the orchestrator decides whether the spec's "corrective turn keeps the RAW" clause is a defect to fix or a law to rewrite.
- 2026-09-16 · step2 · FACT · an ask-human pause MID-BATCH: the rows of every call up to and including the asked one are on the pre-pause partial commit; `agent.resume` dispatches only the asked call (`toolCalls.ts` resume door at `pausedAskIndex`, files nothing) and the batch's LATER siblings are never dispatched on resume at all — no tool message, no basis row, their declaration lives only in the emission (pre-existing batch semantics, not findings-specific). So "exactly ONE basis row per toolCallId" holds with the sibling at zero; pinned by §10 and a probe.
- 2026-09-16 · step2 · FACT · `test/modelFacingSurfaces.test.ts` gained the two PRODUCERS rows for `src/core/agent/findings/reserved.ts` with LOCAL `Surface` constants (`RESERVED_ARGUMENT_DESCRIPTION`: tool-description / request-ephemeral; `ALWAYS_ON_INSTRUCTION`: system-text / request-ephemeral — the `AN_EPHEMERAL_SURFACE` precedent in that file) rather than editing `test/helpers/modelFacingClaims.ts`, which this stage did not own; the description row composes EVERY `description` in the reserved schema tree (a walker), the instruction row composes `FINDINGS_INSTRUCTION`; both return `[]` from `unprovable` at their real lifetime (and at `persistent-history`, per reserved.test.ts). `test/modelFacingScan.test.ts` stays green (its ledger is independent of PRODUCERS).
- 2026-09-16 · step2 · FACT · the receipt law holds armed in both chart shapes (`receipt-conformance.test.ts`): `schemaHashes[name]` equals `receiptHash(runId, toolDigestInput(served))`, equals the digest of `withFindingsArgument(served minus _findings)`, AND equals `withFindingsArgument(registryTool.schema)` — so the served schema is exactly the registry schema through the one decorator, the registry object untouched; `system.pieces` carries `receiptHash(runId, FINDINGS_INSTRUCTION)` with `source: 'instructions'` on every epoch; no new gap kind was needed.
- 2026-09-16 · step2 · FACT · `durability.test.ts`: under the hosting port the stored conversation carries `findingsLedger` only after a turn that recorded a row (every earlier `'sync'` write is a prefix or has no key), an ARMED second standing agent re-seeds it as a record (no event) and stores it forward, and an UNARMED second agent over the same store neither restores nor re-writes it — the envelope it stores back has no `findingsLedger` (the arm gates every read and write; the record survives only in the armed agent's envelope). Pinned as the law's consequence, worth a sentence in the README/design page (docs stage).
- 2026-09-16 · step2 · FACT · `tsconfig.json` excludes `test/`, and under a scratchpad tsconfig that extends it the repo's OWN `receipt-conformance.test.ts` (25) and `durability.test.ts` (3) already fail strict checks on lines this stage never touched (`receipt.cache` possibly undefined, provider stub return types); the nine touched files were typechecked that way with the error set byte-identical before/after (baselines kept in the scratchpad), the new code adding none. eslint clean; prettier `--write` was run on the six files it flagged (insertions only in the diff).
- 2026-09-16 · step2 · FACT · `unsupportedArgument.test.ts`: assertion text (`4417-ganymede`, grounded only in the model's own prose) inside `_findings.previous` on an ARMED `argumentsFrom` tool files NO `unsupported-argument` finding and the disposition row reads checked-pass; the control (the same id as the real `machine` argument, same armed agent) still files ONE finding whose witness never mentions `_findings` — the choice seam reads the peeled args and is still live.
- 2026-09-16 · step2 · workflow `wf_ba1f945a-d23` ended on the session limit after Leaf → Review 1 (2 majors, fixed) → Wire → Prove (byte identity + bench + docs done; the integration-tests agent died mid-run — its edits are on the tree and green); Review 2 (Opus + Sonnet), fixes and the gate are OWED before this step is committed.

- 2026-09-16 · step2 · review 2 · FIXED (blocking) `toolCalls.ts · buildToolCallsHandler · execute` policy-halt block wrote `scope.policyHaltArgs = tc.args` (the emission) — now `callArgs`, the carrier law every pause carrier follows, so `PolicyHaltError.proposed.args` / `sequence[].args` (the appended proposed entry) and the committed `policyHaltArgs` hand the app the peeled args; the basis row and history's verbatim `toolCalls[].args` are untouched. Pinned: `findings-ledger.test.ts` §12 (armed agent, `permissionChecker` halting a declaring call; the case fails on the reverted line). FACT: `PolicyHaltError.sequence`'s EARLIER entries and the checker's own `sequence` are `extractSequence(history)` — the emission, verbatim by law — only the appended proposed entry came from the carrier.
- 2026-09-16 · step2 · review 2 · FIXED (major) the peel was keyed on the arm alone, so an author's `_findings` on a provider/MCP tool was stripped before `execute` although the schema promised it — `reserved.ts · ownsReservedArgument` is now the ONE predicate (asked by `withFindingsArgument`, `buildToolRegistry · assertReservedArgument` and the new `toolCalls · peelCall`, which resolves the tool answering the name and skips the peel when it owns the property; the standings pass and the pauseHere resume door ask the same). `callLLM`'s choice seam stays unconditional by construction (`toolGrounding` is registry-only and an armed registry tool never owns the name — comment added). Pinned: `findings-ledger.test.ts` §13 (`.toolProvider(staticTools([...]))`, one tool owning the name beside one that does not: served undecorated vs decorated, `execute`/`tool_start` see the author's value, one basis row). Docs aligned: design page, CHANGELOG, findings README.
- 2026-09-16 · step2 · review 2 · FIXED (major + blocking, one defect) the corrective turn quoted the PEELED failed answer — ruled a defect against the spec's MUST NOT, not a law to rewrite: `route.ts · buildEnforcingDecider` keeps the emission aside when the arm peeled a key and every RE-ASK exit (`output-retry`, and the same law's other two doors `step-nudge` and `evidence-recheck`, which push `llmLatestContent` into history / the rejected draft) restores it through one `reAsk` helper before routing; every `final` still stands on the peeled JSON, and the route stage's commit is unchanged (raw → peeled → raw is no net write). `outputRetry.ts` comment names the contract. Pinned: §11 made EXACT (`failedTurn.content === FIRST`, key present; the correction quotes the shape error, never the key) and new §14 (evidence-recheck: `evidenceRecovery.instruction` and the served system prompt quote the emission; step-nudge: the premature assistant turn in history is the emission). Design page "no-outputSchema last-batch law" rewritten to the true behaviour: `outputAttempts` records the verdict, not the text. All five new/changed cases fail on the reverted lines (checked by a scripted revert-run-restore; checksums equal after).
- 2026-09-16 · step2 · review 2 · FIXED (major) `docs-next/content/docs/build/agent.mdx` `.findings()` example used a builder shape that does not exist (`new AgentBuilder().llm(...)`) — now `Agent.create({ provider, model }).tool(search).tool(fetchPage).findings().build()`, the shape the tests and the builder's JSDoc use.
- 2026-09-16 · step2 · review 2 · FACT (not in this round's findings, left standing for the orchestrator): (1) `callLLM` choice seam's `declaredEnumValuesOf(schemaOf.get(c.name))` walks the SERVED (decorated) schema, so under the arm the reserved property's enum words (`direct`, `exploratory`, `low`, `medium`, `high`, `fact`, `open`, `noise`, `ruled-out`) join the declared-enum excusal set of every armed tool — a model argument value equal to one of those words would be excused from the unsupported-argument check; fix = walk the schema minus `_findings`. (2) `Agent.checkpoint()` appends `lastRunAnswer` — the PEELED final answer — as the conversation carrier's assistant turn (the worklog's earlier FACT); the answer that stands is peeled by law, but a continued conversation's provider request then echoes a turn that differs from the emission by the key.
- 2026-09-16 · step2 · review 2 (Opus laws · Sonnet tests/docs · spec coverage): 5 blocking/major fixed by the fix agent (policy-halt carrier peeled; re-ask exits restore the emission; per-call peel honours an author-owned `_findings`; docs example; design paragraph); gate green (661 files / 11002 tests, bench columns unchanged). Follow-up closed by hand: the enum fence of the unsupported-argument check now reads the undecorated schema. Rulings on the checkpoint carrier, `sequence`, and the three re-ask doors written on the design page.
- 2026-09-16 · step2 · FACT · a gate that runs `npm run build` beside the suite turns `test/lib/trace-toolpack/browserGraph.test.ts` red (it walks `dist/esm`, which the build deletes first) — three false reds on the landing gate; the build runs BEFORE the suite, never concurrently. Also closed by hand after review 2: the enum fence of the unsupported-argument check reads the undecorated schema (`reserved.ts · withoutFindingsArgument`, pinned in `test/integrity/unsupportedArgument.test.ts`).

- 2026-09-16 · step3 · FACT · types + run constant stage: `Agent.ts`'s findings gate is the private field `findingsOptions` (the brief wrote `this.findings`); `findingsServe: this.findingsOptions.serve ?? 'ledger-and-facts'` is threaded INSIDE the existing armed spread at the `buildSeedStage` deps site (one gate, value-conditional), not as a second spread.
- 2026-09-16 · step3 · FACT · `TypedScope<T>` is `T & ScopeMethods`, so `seed.ts` cannot write `scope.findingsServe` unless `AgentState` declares it — `src/core/agent/types.ts · AgentState.findingsServe?: 'ledger-and-facts' | 'ledger-only'` was added beside `forcedOutputToolName` (the brief's owned-file list omitted the declaration; the spec names only the seed write). Seed writes it under `deps.findings === true && deps.findingsServe !== undefined` — the `forcedOutputToolName` shape, armed only.
- 2026-09-16 · step3 · FACT · an ARMED agent's committed key set now differs from the unarmed twin by `findingsServe` on every run, plus `findingsLedger` when the model declared something — `test/core/agent/findings-ledger.test.ts` pins the old "exactly one key" law twice (`armed-but-silent: same committed keys as unarmed` → `expect(keysOf(on)).toEqual(keysOf(off))`; `keys(on) is keys(off) plus findingsLedger` → `[...keysOf(off), 'findingsLedger']`) and both now fail by that one key; the new law is pinned in `findings-declarations.test.ts` (`[...keysOf(off), 'findingsServe']` when silent). Fix in the ledger test: add `'findingsServe'` to both expected lists.
- 2026-09-16 · step3 · FACT · `ContextSource += 'findings'` compiles clean: no exhaustive `never` switch or full `Record<ContextSource, …>` exists in `src` (only `Partial<Record<…>>` in `slots/helpers.ts`, `recorders/core/types.ts`, `events/payloads.ts`) nor in the lens (`servedGraphAt.ts` keys its maps by `ServedSlotName`, and `ServedSourceName` is only a pass-through alias). No test pins the union's members — `test/recorders/contextEngineering.test.ts · known` already omits `'evidence-recovery'`. Open sets that classify by name and do NOT yet know `'findings'`: `recorders/core/contextEngineering.ts · ENGINEERED_SOURCES` (has `'evidence-recovery'`; without the entry the piece is routed to NEITHER `onEngineered` nor `onBaseline` — the documented forward-compat slot) and `lib/context-bisect/localize.ts · ENGINEERED_SOURCES` (a string set without `'evidence-recovery'` either).
- 2026-09-16 · step3 · FACT · `readRunConstant` is exported from `src/lib/time-travel/index.ts` but NOT from the root barrel (`epochAt` / `epochLocations` are); the test imports it by the internal path, as sibling tests import internals.
- 2026-09-16 · step3 · FACT · `bench/findings-shuffle.mjs` (new): the SHUFFLE harness — one paging tool (`next_record`, cursor k serves the k-th record of THIS RUN'S order), FACTS planted p95 readings + NOISE inventory rows, a seeded Fisher–Yates order per run (mulberry32 from `AF_SHUFFLE_SEED` × run, the SAME order for run r in all three conditions — a paired comparison), conditions findings off / `.findings({ serve: 'ledger-and-facts' })` / `.findings({ serve: 'ledger-only' })`, columns facts-in-answer · noise-cited · declared (standing rows that named a result, over records) · drift (distinct claim sets / runs — the normal form is `node-<i>=<value>` pairs + cited SKUs, sorted, so wording and order do not count) · unknown-id-standings. `AF_SHUFFLE_PROVIDER=mock` (default) scripts compliance and ECHOES every reading and SKU it is served (system prompt + tool messages), so the columns measure the harness and the wire; `=ollama` uses the package's own `ollama(AF_SHUFFLE_MODEL)` (`OLLAMA_HOST` read by `OllamaProvider · resolveBaseUrl`), `temperature: 0`. Green on the mock today: `node bench/findings-shuffle.mjs` → off 1.000/1.000/-/0.20, ledger-and-facts 1.000/1.000/0.944/0.20, ledger-only 1.000/1.000/0.944/0.20 (18 records, 5 runs, seed 20260916); no real-model number exists yet.
- 2026-09-16 · step3 · FACT · `bench/findings-context.mjs` runs its `main()` at module load and exports nothing, so the shuffle harness DUPLICATES the planting shape (a node's p95 as the fact, an inventory row as the noise) rather than importing it; `bench/README.md` does not exist, so the harness's paragraph is its comment header. `package.json` (not this stage's file) has no `bench:findings:shuffle` script — `"bench:findings:shuffle": "node bench/findings-shuffle.mjs"` beside `bench:findings` is the orchestrator's one-line call.
- 2026-09-16 · step3 · FACT · the `serve` dial is accepted by `AgentBuilder.findings(options)` on this tree but INERT (no `findings/serve.ts` yet), so all three conditions print identical numbers on the mock today; the harness's smoke test pins only what holds BEFORE and AFTER the wire lands (off echoes every fact and every SKU; armed rows declare N−1 of N — the last-batch law; facts-in-answer 1.0 on every armed row; drift 1/runs for an echo) and deliberately does NOT pin `noise-cited` on the armed rows — that is the number the wire moves. Its first cut refused `1007us` (`\b` between `7` and `u`) and printed facts 0 / drift 1: the pinned smoke test caught the harness's own defect before any model was involved.
- 2026-09-16 · step3 · FACT · `OllamaProvider` synthesizes tool-call ids (`ollama-call-N`), so whether a real model can NAME a result's id in `_findings.previous` on that wire is exactly the design page's second, unproved provider assumption; the harness counts standing rows with `unknownId` in their own column (`unknown-id-standings`) rather than folding them into `declared`, so a real-model run shows the assumption's failure instead of hiding it.
- 2026-09-16 · step3 · FACT · `findings/serve.ts · findingsLedgerPiece` takes an optional THIRD argument `served: readonly string[]` — the wire's `role: 'tool'` ids, from the exported `servedToolCallIds(messages)` — because the spec's two-argument signature cannot name `undeclared` results (a pure function cannot see the wire). Both `callLLM · buildCallLLMStage` and `servedView · viewOf` must pass `servedToolCallIds(stripFrameworkFields(history))`; the list is identical before and after `collapseJudged` (an id is never rewritten). Without it the `undeclared:` line is omitted, the same rule every empty bucket follows.
- 2026-09-16 · step3 · FACT · `iteration` is the header's anchor ("composed for iteration N" — the placement ticket's "every clause anchored to the call it was composed on" law); the spec grammar names no other use. Consequence: the piece's bytes, and so its receipt hash, differ per iteration even for an unchanged ledger.
- 2026-09-16 · step3 · FACT · the 64-line cap is applied to ALL FOUR text buckets (the spec named facts and refs only) so `limitations` and `nextSteps` are bounded by the same number, overflow stated as `+K more (cap 64)`; the 240-char clip is applied to every text-bucket LINE as a whole (fact, conflict, ruled-out, open, next-step) with newlines in the model's text folded to spaces first, stated as `…[clipped N chars]`; count lines (noise, undeclared) are bounded by the 32-id cap and `+K more` instead. Exported as `FINDINGS_PIECE_LIMITS`.
- 2026-09-16 · step3 · FACT · a conflict line's witnesses are resolved by key over the CURRENT fact rows (a local twin of the module-private `ledger.ts · witnessesOf`, one entry per matching assertion in fold order), rendered `tool:<id>[ | artifact:<ref>]` exactly as their fact lines — not the assertion's own `provenance` string, which reads `artifact:<ref>` alone for a placed witness. A row with `unknownId` renders ` (unknown id)` after its pointer and no tool name (never 'unknown tool'); a fact row that asserts nothing renders `(fact declared, nothing asserted) ← tool:<id>`; a ruled-out row with no `line` renders without the colon.
- 2026-09-16 · step3 · FACT · `collapseJudged` is idempotent by construction: a tool message whose content already equals its ticket is not a change, so a second pass returns the SAME instance. The ticket's key order is pinned as `{"collapsed":true,"standing":…,"toolCallId":…[,"ref":…]}` and `isCollapsedToolResult` (the `isPlacedToolResult` twin) is exported for the bench/lens.
- 2026-09-16 · step3 · FACT · nothing exports `serve.ts` yet: no barrel (`src/index.ts`, `src/doors/*`) names `findingsLedgerPiece` / `collapseJudged` / `servedToolCallIds` / `isCollapsedToolResult` / `FindingsServeMode` / `FINDINGS_PIECE_LIMITS`; `AgentOptions.findings.serve` in `core/agent/types.ts` spells the union inline and could alias `FindingsServeMode`. The wiring stage decides; the docs-truth ratchet will count any root export it adds.
- 2026-09-16 · step3 · FACT · the piece's library-authored words pass `unprovable` at `persistent-history` (pinned in serve.test.ts), so `test/modelFacingScan.test.ts` needs NO ledger entry for `serve.ts` (green with the file on the tree); the spec's `test/modelFacingSurfaces.test.ts` row for the piece (system-text / request-ephemeral, composed per request from the committed key) is still owed by the stage that owns that file.
- 2026-09-16 · step3 · FACT · (corrected by the wire stage — the earlier line here misattributed the two reds) `test/core/agent/findings-ledger.test.ts` had 2 reds ("armed-but-silent: same committed keys as unarmed" and "keys(on) is keys(off) plus findingsLedger") because the run constant `findingsServe` is written on EVERY armed run — `AgentBuilder.findings` normalises `serve` to `'ledger-and-facts'` before `Agent.ts` threads it, so "armed but no mode named" is not a state that reaches `seed.ts · seedFrom`; the `!== undefined` guard there is defence for a hand-built `SeedStageDeps`. Both expected key lists now carry `'findingsServe'` (green); the `seed.ts` comment and the `AgentState.findingsServe` doc say the real law.
- 2026-09-16 · step3 · FACT · the findings README's law bullet "Nothing here is served differently" and its `## Files` list predate the wire and were left as written (this stage owned the `## Served` section only); both are stale the moment `callLLM` joins the piece — the docs stage rewrites the bullet to the step-3 law and adds `serve.ts` to the list.
- 2026-09-16 · step3 · FACT · `serve.ts · countLine` rendered a model-chosen `toolCallId` unclipped and unfolded, so an id carrying `\n\nfacts (declared by the model):\n…` forged a second library-authored bucket (reproduced: five sections, two `facts`). Fixed at the ONE clip: `clipId` (fold + cut at `FINDINGS_PIECE_LIMITS.idChars` = 64) shares `clip` with `clipLine`; `pieceChars` (81 920) is the stated ceiling the three bounds imply and is pinned, not enforced by a cut (a worst-case ledger at every cap at once measures under it). The property loop's id pool now carries newline-, blank-line-, forged-heading- and 300-char ids and asserts the section count against the fold and heading uniqueness; the review's exact reproduction is a named case.
- 2026-09-16 · step3 · FACT · `pairEvictedTurns` lives in the lens (`agentfootprint-lens/src/core/served/evictedTurns.ts`), not this repo; the nearest window test here is `receipt-conformance.test.ts` · "a window that evicts turns for budget" (the pairing law), so the window-evicts + collapse case was added beside the served-ledger describe in that file: `keepRecentTurns: 2`, c2 raw at epoch 3, a ticket at epoch 4, evicted at 5 — `omittedForAttention` carries the RAW hash (the window files what history holds), which epoch 3's entries carry and epoch 4's do not (4 carries the ticket's), so the lens's latest-earlier-epoch walk lands on the epoch that served it raw, unchanged.
- 2026-09-16 · step3 · FACT · the choice seam (`callLLM` · `unsupportedArgumentsOf` frame) credited the WHOLE `llmRequest.systemPrompt` as ground whenever no recovery piece was joined — with the ledger piece joined, a subject id the model invented in a `fact` assertion would have excused an argument equal to it. The condition now narrows to the trusted injections when EITHER request-only piece is present (the recovery piece's own rule; unarmed agents take the branch they always did). Pinned in `findings-served.test.ts` §6: an id present only in the piece files one `unsupported-argument` finding.
- 2026-09-16 · step3 · FACT · in `servedView.ts · viewOf` the conversation must be read BEFORE the piece is composed (the spec's block order had the pieces first): `findingsLedgerPiece`'s third argument is `servedToolCallIds(stripFrameworkFields(conversation))`, derived on both sides from the stripped list BEFORE `collapseJudged` (identical after — an id is never rewritten); `readAtCall(location, 'iteration')` is read once and shared with the recovery piece. `servedModeOf` narrows the run constant: only the literal `'ledger-only'` changes anything, everything else is the default the wire falls back to — so a subtree without its run log (`no-run-log`) rebuilds identically on the default mode; under the bench-gated mode its `messages.entries` would show fact results verbatim where the receipt hashed tickets — written into the gap's MECHANISM comment, not widened into its `fields`.
- 2026-09-16 · step3 · FACT · the grouped chart's LLM_CALL `inputMapper` carries `findingsLedger` value-conditionally (beside `compactions`); no outputMapper line, because tool-calls and the Route decider write the key on the OUTER scope. Pinned by `findings-served.test.ts` §1: `system.text`, `messages.asSent`, the piece list AND the raw wire requests are byte-equal between `dynamic` and `dynamic-grouped` at every epoch.
- 2026-09-16 · step3 · FACT · `agent-findings.json` regenerated ALONE (15 `cmp`-equal after; 17/17 green): 14 moved paths in three families — the run constant `findingsServe` with its one `set` trace row on seed's commit; the findings piece on the epoch-3 receipt (`system.pieces` 3 → 4, `chars`/`hash`, `requestMeasurement` ×4) and the served `system.text`; nothing else. NO `asSent`/`messages.entries` moved: the scenario's only noise standing is declared on the ANSWER (after the last wire) and `c1` is a fact served verbatim, so the spec's "asSent shows collapsed entries" does not occur in THIS scenario — the collapse is pinned by `findings-served.test.ts` and `receipt-conformance.test.ts` instead; the scenario was left as step 2 generated it (its 104-path step-2 delta stays documented). Header of `byte-identity.test.ts` names the delta.
- 2026-09-16 · step3 · FACT · the `integrity.context_error` payload is the `ContextError` spread (`seam`, `predicate`, `subject: { kind: 'tool', id }`, `message`, …) plus `iteration` — no top-level `toolName`; the served test asserts on `seam`/`predicate` and the message naming the tool, the `unsupportedArgument.test.ts` shape.
- 2026-09-16 · step3 · FACT · `test/modelFacingSurfaces.test.ts` gained the PRODUCERS row for `src/core/agent/findings/serve.ts` (`LEDGER_PIECE`: system-text / request-ephemeral; `lifetimeBecause` = composed per request from the committed key, joined into `systemPieces` only, never an injection or a history turn) composing a hand-built ledger that reaches every arm (conflict, placed fact, open + settles, ruled-out, noise, undeclared, `+K more`, `…[clipped]`); `unprovable` returns `[]`; `modelFacingScan.test.ts` stays green.
- 2026-09-16 · step3 · FACT · files touched outside the brief's owned list, each one line: none in src; `docs/design/2026-09-findings-ledger-worklog.md` (this file). NOT touched and still owed elsewhere: the findings README's "Nothing here is served differently" bullet and `## Files` (docs stage); `package.json` `bench:findings:shuffle`; `bench/findings-context.mjs` rewrite to `servedAt` (the bench packet); `CHANGELOG` 9.101.0 entry for step 3.
- 2026-09-16 · step3 · docs stage · `bench/findings-context.mjs · servedAtAnswerTurn` rewritten to the served view (`epochLocations` → last epoch → `servedAt(snapshot, k)`: `system.text`, `messages.asSent`, `messages.requestOnly`; `receiptAt` for the count); columns `facts-verbatim` / `facts-in-piece` / `noise-verbatim` / `noise-tickets` / `noise-share` (verbatim noise chars over ALL wire tool-message chars, tickets included) / `wire-tool-bytes` (the denominator, printed); rows none / sliding / none+findings / sliding+findings / none+ledger-only; the step-2 law kept as its own check with the arm SILENT (`.findings()` on, the script declaring nothing) and `process.exitCode = 1` on a moved column. Printed (20 calls, keep 6): none+findings facts-in-piece 6, noise-tickets 13, noise-share 93.5% → 15.2%, wire-tool-bytes 2322 → 1028; sliding+findings facts-verbatim 2 but facts-in-piece 6, share 92.3% → 41.5%; tool-msgs and receipt-msgs unmoved (20/41, 6/13); the law holds. Table pasted verbatim on the design page under "Step 3 — served from the ledger" with the reading; the Track line ticks 3; the Status paragraph names both steps on the one unreleased 9.101.0 entry.
- 2026-09-16 · step3 · FACT · the bench's `facts-in-piece` matches a planted fact by its ASSERTION — `node/node-<k> · p95 = <planted value>` in `system.text` — not by the `FACT-<k>` label: the piece quotes the model's declaration, never the tool's string, so the label cannot appear there and a wrong value would not count. `noise-tickets` parses the ticket by hand (`collapsed === true`, string `standing` and `toolCallId`): `isCollapsedToolResult` is on no barrel (the docs-truth ratchet counts every root export), and package `exports` does not open the internal path.
- 2026-09-16 · step3 · FACT · under `'ledger-only'` the wire's tool-result bytes are HIGHER than under the default (1205 vs 1028 in the print): the bench's planted fact (`FACT-3 node-3 p95 1021us`, 24 chars) is shorter than a fact ticket (`{"collapsed":true,"standing":"fact","toolCallId":"c3"}`), so the dial saves bytes only when facts are large. The `extra-output-tokens` column was dropped (0 by construction of the mock — the step-2 reading); `request-only lines at the answer turn` is printed (0 on every row) rather than assumed.
- 2026-09-16 · step3 · FACT · `bench/findings-shuffle.mjs` (not this stage's file) exits 1 on the mock post-wire: its smoke test expects `drift` 0.2 (= 1/runs) on the armed rows, the print says 0.8 on both, `noise-cited` 0.600. Verified in the `AF_SHUFFLE_VERBOSE=1` print: the LAST record of every order is undeclared (the last-batch law) and served in full, and the echo cites its SKU exactly when it is noise — runs 0/2/4 end on records 10/14/16 (noise, one SKU each), runs 1/3 on records 5/2 (facts, none) — four distinct claim sets, order-dependent by construction of the WIRE, not of a model. The expectation was written on the tree where the dial was inert; the harness's owner changes its armed-row expectation to what the last-batch law implies (or leaves the undeclared last record out of the echo). `facts-in-answer` 1.000 and `declared` 0.944 on the armed rows are as the smoke test expects. Both prints are on the design page, labelled the mock; NO real-model run has happened and the `serve` default is untouched. `package.json` still has no `bench:findings:shuffle` script.
- 2026-09-16 · step3 · FACT · the grammar example on the design page and in `findings/README.md` was GENERATED by `dist/core/agent/findings/serve.js` (a hand-built ledger: two disagreeing fact rows, one open, one ruled-out, one noise; one undeclared result on the wire; iteration 4), pasted as printed; the same script confirmed `collapseJudged` returns the same instance with no ledger and is idempotent over its own output.
- 2026-09-16 · step3 · docs stage · `findings/README.md`: the stale "Nothing here is served differently" law rewritten to the step-3 law, the "bench decides" bullet to the silent-arm law, `serve.ts` added to the Walker line and `## Files`, the `## Served` section shortened to WHY + the generated example. `CHANGELOG.md`: package.json is 9.100.0 so the 9.101.0 entry is unreleased — a second "### Added — served from the ledger" block added under it, the first block's "nothing is served differently yet" and "`serve` … inert" sentences corrected, the Unchanged block's bench sentence rewritten to the silent-arm law and the regenerated reference. `docs-next/content/docs/build/agent.mdx`: "Findings on the record" gains "What the answer turn is served" (the piece, the ticket, `servedAt`, the `serve` dial with `'ledger-only'` named bench-gated, `keepLedgerFacts` inert); no new root export to name.
- 2026-09-16 · step3 · review 2 · finding 1 (the typed surface said `serve` is inert) — REAL, fixed at the three doc sites: `src/core/agent/types.ts · AgentOptions.findings` (the block and the `serve` field comment), `src/core/agent/AgentBuilder.ts · findings` ("WHAT IT DOES NOT DO YET" → "WHAT THE ANSWER TURN IS SERVED"), `src/core/Agent.ts · findingsOptions`; each now says `serve` is live (`'ledger-and-facts'` default; `'ledger-only'` bench-gated on `bench/findings-shuffle.mjs`) and only `keepLedgerFacts` is inert until standing-aware eviction — the docs-next wording. Why nothing caught it: `npm run docs:truth` scans docs-next prose, not JSDoc. Pinned by `test/core/agent/findings-served.test.ts` §8, which reads the comment block before each of the three anchors and refuses "inert until step 3" / "both are inert" / "WHAT IT DOES NOT DO YET" / `serve`…inert, and requires `keepLedgerFacts`…inert; its first run caught a semicolon in the Agent.ts sentence (the pin reads a sentence per fact, so the doc got one).
- 2026-09-16 · step3 · review 2 · finding 2 (the shuffle harness exits 1 on its own smoke test; the page handed the fix elsewhere) — REAL, the file was `??` in this tree. `bench/findings-shuffle.mjs · smokeCheck(rows, records)` now COMPUTES the armed-row expectations from the orders and the last-batch law (`noise-cited` = orders ending on a noise record / runs; `drift` = distinct SKUs at the last position + 1 if any order ends on a fact, over runs); the echo is unchanged (it must answer from what it was served or the columns stop measuring the wire); the green message names the law. `package.json` gained `"bench:findings:shuffle": "node bench/findings-shuffle.mjs"` (one script line, outside step 3's touched list — the finding's fix named it). Print after the fix (mock, seed 20260916, 5 runs): off 1.000/1.000/-/0.20, ledger-and-facts 1.000/0.600/0.944/0.80, ledger-only 1.000/0.600/0.944/0.80, smoke test green, exit 0 — pasted on the design page in place of the red block, the reading paragraph rewritten, the ownership sentence corrected. Still no real-model run; the `serve` default untouched.
- 2026-09-16 · step3 · review 2 · finding 3 (the header's iteration anchor made an armed agent's cached system prefix miss on every call, and the cost was never named) — REAL. `findings/serve.ts`: the header is a constant `HEADER`; `findingsLedgerPiece(rows, served)` — the `iteration` parameter is gone at both call sites (`callLLM · buildCallLLMStage`, `servedView · viewOf`; `iteration` is still read there for the recovery piece); "The cache" section in the module doc names the seam (`CacheDecisionSubflow · computeCacheMarkers` index 0 of the system slot under `systemPromptCachePolicy` `'always'`; `anthropicCacheWire · applyCacheMarkers` marks the whole joined prompt as one block). FACT found on the way: the first rewording, "composed from the record of this run", tripped `test/modelFacingScan.test.ts`'s tool-result lifetime rule (`\bthis (session|run|conversation)\b` without "that call") and `serve.test.ts`'s `unprovable` case — the header is "composed from the record" now. Pins: `serve.test.ts` "no per-call byte" (same inputs → same bytes; section 0 is `HEADER`; no "iteration"), `findings-served.test.ts` §7 (an `output-retry` re-ask is served the same piece, the same `systemPrompt` and the same `receipt.system.hash` as the epoch before it). `agent-findings` regenerated ALONE (15 `cmp`-equal; the moved set against the step-2 reference is the SAME 14 paths, values only: the header at its two sites, the piece's `chars`/`hash`, `system.chars`/`hash`, four `requestMeasurement` sizes); header of `byte-identity.test.ts` names it. Design page "The prompt cache (second review)" and the CHANGELOG 9.101.0 block name the trade: a declaring model moves the ledger every call, so every such call writes a new system cache entry and reads none (and on Anthropic's wire the message breakpoints behind it miss) — a wire-bytes claim, not a cache claim; no bench in the tree counts cache tokens. `npm run bench:findings` after the rebuild: print byte-equal to the table on the design page (string-compared), so the table stands.
- 2026-09-16 · step3 · review 2 · finding 4 (`types.ts · AgentOptions.findings.serve` says inert) — REAL, the same defect as finding 1 at one of its three sites; fixed and pinned by the finding-1 line above.
- 2026-09-16 · step3 · review 2 · finding 5 (`AgentBuilder.findings` "WHAT IT DOES NOT DO YET") — REAL, the same defect as finding 1 at another of its three sites; fixed and pinned by the finding-1 line above.
- 2026-09-17 · step4 · FACT · `window/ledgerFactPins.ts · LedgerFactPin` carries `toolName` and `chars` beyond the spec's three fields (`toolCallIds`, `turnIndex`, `messageIndex`) — the code's shape, not the spec's: `turns.ts · admitPins` maps `toolName/turnIndex/chars` straight off a pin into `WindowObservations.pinned`, which is the shape `WindowRecord.ledgerFacts` reuses, and under `noUnusedParameters` the spec's `history` parameter is load-bearing only through `toolNameOfMessage`. `toolCallIds` lists EVERY result the turn holds, in wire order — the per-turn `standingOf` cannot say which member is the fact, and the turn is what the pin holds (a noise result answered beside a fact stays with it; each id's own standing is on the ledger). A fact turn nothing can name is not pinned (the `toolNames.ts` law — the record must not name a tool that does not exist). `turnChars` is duplicated module-private (lastToolResult.ts is not this stage's file; a later pass may hoist one into turns.ts).
- 2026-09-17 · step4 · FACT · `turnStandingOf(turn, standingOfCall)` takes a per-ID lookup `(toolCallId) => Standing | undefined` — the shape of `foldLedger(rows).standingOf.get(id)?.standing` — so the stage binds `standingOf = (turn) => turnStandingOf(turn, (id) => fold.standingOf.get(id)?.standing)`; `rankStanding` returns a number ONLY to compare (`fact` 4 > `open` 3 > undeclared 2 > `ruled-out` 1 > `noise` 0; never recorded), and undeclared ranks above the two verdicts on purpose so a batch with one unjudged member is undeclared, not noise. Both helpers are exported from the MODULE only; the window barrel exports `LedgerFactPin` + `ledgerFactPinsOf` only; `src/index.ts` is a NAMED list (no `export *` over the window barrel), so no root export moved in this stage and `docs:truth` is untouched — the root barrel line is the threading stage's.
- 2026-09-17 · step4 · FACT · no `Record<WindowRefusalReason, …>` exists in src, test, docs-next or the lens, so `'ledger-fact'` is additive-safe at runtime; `WindowStrategyInput.standingOf?` is optional and typed over the same `(turn: Turn) => Standing | undefined` the pin takes, so the stage passes ONE function to both.
- 2026-09-17 · step4 · FACT · `npm run test:types` is RED on this stage at exactly one line, by that test's own design: `test/type-regressions/WindowStrategy.assignability.test.ts · describeReason` is an exhaustive `never` switch over `WindowRefusalReason` ("a deliberate, compile-time break for a consumer who narrows the whole union — the 9.55.0 precedent"), and `'ledger-fact'` is a new member — `WindowStrategy.assignability.test.ts(305,17): error TS2322: Type 'string' is not assignable to type 'never'`. The owed change is one arm, `case 'ledger-fact': return 'a declared fact';` beside the `'last-tool-result'` arm, in a file outside the pin stage's list; its RUNTIME assertions (vitest) pass on this tree (6 window files, 108/108) and `npx tsc --noEmit -p tsconfig.json` (src) is green after each pair.
- 2026-09-17 · step4 · FACT · option + thread stage: `requireKeepLedgerFacts`, `DEFAULT_KEEP_LEDGER_FACTS` (= 4) and `resolveKeepLedgerFacts` live module-private in `src/core/Agent.ts` beside their one call site, NOT in `window/options.ts` beside `requireKeepLastToolResults` / `DEFAULT_KEEP_LAST_TOOL_RESULTS` — `window/` was outside this stage's fence and a sibling stage was editing `window/types.ts`, `window/strategy.ts`, `window/index.ts` and adding `window/ledgerFactPins.ts` in the same checkout while this ran. Consequence for the wiring stage: the ONE default is applied at the resolution site (the `Agent` constructor) and threaded as a plain number, so `WindowStageDeps.keepLedgerFacts` is present exactly when `hasFindingsLedger` is and the stage applies NO default of its own (absent = unarmed = no hold; `0` = hold off). Moving the three helpers to `window/options.ts` is a follow-up the wiring stage may take.
- 2026-09-17 · step4 · FACT · the `AgentOptions.findings.keepLedgerFacts` field doc said "`false` for all"; it is `false` = `0` = no hold (the spec's law and `keepLastToolResults`'s) — corrected at the field. Resolution: `findings({ keepLedgerFacts })` over `AgentOptions.keepLedgerFacts` with `??`, so a door `false` still wins over a named option (pinned).
- 2026-09-17 · step4 · FACT · `test/core/agent/findings-served.test.ts` §8 pins each of the three doc sites (`types.ts · AgentOptions.findings`, `AgentBuilder.findings`, `Agent · findingsOptions`) to `/keepLedgerFacts[^.]*\binert\b/`. Kept green by ending each block with "until the hold lands in `stages/window.ts` … `keepLedgerFacts` is inert there"; the same sentence sits on the new `AgentOptions.keepLedgerFacts` field and in the test header. The stage that makes the hold live must, in ONE packet: delete those five sentences, flip §8's `toMatch` to `not.toMatch`, and rewrite `docs-next/content/docs/build/agent.mdx` ("accepted and inert") — or §8 goes red / the docs lie.
- 2026-09-17 · step4 · FACT · `Agent.buildChart` runs at construction (`initChart` in the constructor), so `buildWindowStage`'s deps are observable at `build()` with no run: `findings-declarations.test.ts` wraps the module export in a pass-through `vi.fn` via `vi.mock(…, importOriginal)` (the `milestone-stops-contract.test.ts` precedent) and reads `mock.calls[i][0]`; the record does not move in this stage (pinned: same `keysOf` on an armed windowed run with the ceiling named or not). Nothing on `WindowStageDeps` is exported from a barrel, so no docs-truth ratchet moved.
- 2026-09-17 · step4 · FACT · the top-level `AgentOptions.keepLedgerFacts` is validated in the `Agent` constructor whether or not `.findings()` is on (a silently ignored dial is a configuration mistake — the `requireKeepLastToolResults` rule) and is ACCEPTED-and-inert without `.findings()` (the keepLastToolResults-without-a-window precedent), not refused; `AgentBuilder.findings`' inline check is unchanged (its message pinned loosely by `findings-ledger.test.ts` `/keepLedgerFacts/`), the `Agent` message mirrors `requireKeepLastToolResults`'s.
- 2026-09-17 · step4 · FACT · files touched by this stage: `src/core/agent/types.ts`, `src/core/agent/stages/window.ts` (two optional deps fields, no behaviour), `src/core/Agent.ts`, `src/core/agent/AgentBuilder.ts` (doc only — its check already existed), `test/core/agent/findings-declarations.test.ts` (+10 tests), this worklog. NOT touched and owed elsewhere: `docs-next/content/docs/build/agent.mdx`, CHANGELOG `## [9.102.0] - 2026-09-17`, `window/README.md` + `strategies/README.md` law with example, design page Step 4 status. Gate: `npx tsc --noEmit -p tsconfig.json` EXIT 0 after files 2 and 4; `findings-declarations` 19/19; `agent-window-strategies` + `window-last-tool-result` + `findings-served` + `findings-ledger` + `byte-identity` 116/116 in 5 files, the 16 references untouched (`git status test/core/tools/reference/` empty); eslint on the five files 0 errors (7 pre-existing warnings, none on added lines).
- 2026-09-17 · step4 · FACT · engine + stage stage: the stand-down reads REFUSALS, not the observation blocks (the spec's "read BOTH observation blocks" — a block is absent when a pin held nothing, while a refusal is present exactly when a pin blocked a turn, and when nothing left every candidate's refusal is on the record). `stages/window.ts · pinIsBlocking(records, reasons)` takes the pin names it counts: the recency pin's stand-down reads its own name only (`LAST_TOOL_RESULT_PIN` — its rule and its `WindowObservations.standDown` doc are the exact 9.57.0 law), the fact pin's reads the FAMILY (`ANY_PIN` = `'last-tool-result'` and/or `'ledger-fact'`). The family is load-bearing: `refusalFor` reports the recency pin first for a turn held by both, so a fact stand-down that read only its own name never sees that turn blocking and the two pins alternate under each other's name (derived: visits 1–2 `'last-tool-result'`, 3 recency releases → `'ledger-fact'`, 4–5 recency back → `'last-tool-result'`, 6 recency releases again → `'ledger-fact'`, period 3, the window never shrinks). Pinned by `test/core/window-ledger-fact.test.ts` "a turn held by BOTH pins stands down as ONE family — no alternation, ever" (nine visits, streak ≤ 2). When both fire, both blocks are filed with `standDown: true`.
- 2026-09-17 · step4 · FACT · `turns.ts · admitPins` is now `spendCeiling` (one ceiling-spender for both pins; the free-pin law lives once) called twice; `RemovalGuards` gains `ledgerFactPins?` / `keepLedgerFacts?` / `factPinnedTurnIndexes?` (kept apart from `pinnedTurnIndexes` — different ceilings, separate books), `RemovalPlan` gains `ledgerFacts?`. Observable plans are unchanged where no fact pin is contested (`limit 0 plans exactly as it did before the hold existed` deep-equals the no-ledger plan); the only internal change is that a plan with nothing admitted now spreads the guards into a copy where it used to return the same reference — the guards object is recorded nowhere. The fact ceiling is spent on EVERY contested fact turn, including one the recency pin also holds (the spec's free-pin law names only `keepRecentTurns`): such a turn appears in BOTH blocks and reports `'last-tool-result'` (pinned: "the two pins keep separate books").
- 2026-09-17 · step4 · FACT · `stages/window.ts · buildWindowStage` reads `scope.findingsLedger` ONCE under `deps.hasFindingsLedger === true` (spread into a plain array before `foldLedger`, the ledger.ts rule), binds ONE `standingOf` (`turnStandingOf` over `fold.standingOf.get(id)?.standing`) and hands it to both `ledgerFactPinsOf` and `strategy.plan` value-conditionally (`keys(on)` = `keys(off)` + `standingOf`, pinned); `factLimit` = `deps.keepLedgerFacts ?? 0` under the arm, 0 otherwise — no default applied here (the option stage's law). The record gains `ledgerFacts` (held / turned away / stood down) and `droppedStandings` (every evicted `role: 'tool'` message with an id, `standing` absent = undeclared), both armed-only and value-conditional; the unarmed record composition is the expression it was. The gate is pinned by a getter that counts reads of `findingsLedger` on an unarmed stage (0).
- 2026-09-17 · step4 · FACT · leaf findings: #1 and #3 are the SAME real defect (`npm run test:types` RED at `WindowStrategy.assignability.test.ts · describeReason`, confirmed EXIT 2 before the edit) — fixed with `case 'ledger-fact': return 'a declared fact'` plus a membership `it` in the file's grammar; `npm run test:types` EXIT 0 after. #2 (`WindowStrategyInput.standingOf` doc says the stage binds it) — NOT applied as a caveat: this stage binds it, so the sentence is true on this tree.
- 2026-09-17 · step4 · FACT · byte identity: the 16 references run green on the step-4 tree BEFORE the scenario existed (250/250 across the window/findings/integrity files, `git status test/core/tools/reference/` empty); `agent-findings-window` generated ALONE (`AF_TOOLS_REFERENCE=update -t agent-findings-window`, 1 passed / 17 skipped, the 16 copied aside and `cmp`-equal after); the header of `byte-identity.test.ts` names what the reference holds, read back from its bytes (seven records; hold at iteration 4; `c2` noise dropped at 5 with `droppedStandings`; both facts held at 6; `c4` ruled-out, `c5`/`c6` undeclared dropped at 7–9; no `standDown`; no `observations` block; last epoch's wire `user · c1 · c3 · c7 · c8`; two collapse tickets in the served views).
- 2026-09-17 · step4 · FACT · `test/core/window-current-request.test.ts · scripted` gained an optional `argsFor` (default = the empty args every case sent) and `agentFor` an `{ armed }` option (default = the unarmed walk); `window-refusal-rename.test.ts` gained a `declaring` provider and a describe pinning that an UNARMED agent never serializes `ledger-fact` / `ledgerFacts` / `droppedStandings` even when the model emits `_findings`, and an armed one names `'ledger-fact'` beside the reasons it always named (and never the word `disposition`). eslint on the eight touched files: 0 errors; 5 pre-existing `no-non-null-assertion` warnings in `turns.ts` (`segmentTurns` / `planRemoval` lines that predate this stage; none on added lines).
- 2026-09-17 · step4 · FACT · NOT touched by this stage, owed elsewhere now that the hold is LIVE: (1) the five "`keepLedgerFacts` … inert until the hold lands in `stages/window.ts`" sentences (`src/core/agent/types.ts · AgentOptions.findings` block + `AgentOptions.keepLedgerFacts` field, `AgentBuilder.findings`, `Agent · findingsOptions`, the `findings-served.test.ts` §8 header) are now FALSE, and §8 pins them with `toMatch(/keepLedgerFacts[^.]*\binert\b/)` — the one packet the earlier worklog line names (delete the sentences, flip §8 to `not.toMatch`, rewrite `docs-next/content/docs/build/agent.mdx` "accepted and inert"); (2) `src/index.ts` now exports `LedgerFactPin` (a new root export the `docs:truth` ratchet counts) and no docs-next prose names it yet; (3) `window/README.md` + `strategies/README.md` law with an example; (4) CHANGELOG `## [9.102.0] - 2026-09-17`; (5) `src/core/agent/window/types.ts · WindowObservations.standDown` doc still describes the recency pin's rule only ("both named `'last-tool-result'`") — true for `observations`, while `ledgerFacts`' stand-down reads the family as the `WindowRecord.ledgerFacts` doc already says; (6) the bench row (`bench/findings-context.mjs`, 30 calls / fact every 3rd / sliding keep-6) — no number quoted here.
- 2026-09-17 · step4 · FACT · bench + docs stage: `bench/findings-context.mjs` gained the LONG-RUN table (LONG_N = 30 calls, a fact every 3rd, sliding keep-6; rows `sliding` off / `hold=0` (`keepLedgerFacts: false`) / `hold=4` (default) / `hold=6`), two columns read off `scope.compactions` through `agent.getLastSnapshot()` (`facts-held` = `ledgerFacts.pinned.length` on the LAST record; `dropped f/o/n/r/u` = `droppedStandings` summed over every record), a line with `yielded` + stand-down counts, and a step-4 LAW beside the step-2 one (`hold=0` must match the unarmed `sliding` row on facts-verbatim / tool-msgs / receipt-msgs and hold nothing; exit non-zero otherwise); `N`/`calls` threaded per row so the first table stays at 20. As printed (exit 0): facts-verbatim 2 → 2 → 6 → 8, facts-in-piece 0 / 9 / 9 / 9 (the 10th is the undeclared last batch), noise-share 92.3% then 0.0% on every armed row — 0.0% and not the 20-call table's 32.9% because at N = 30 the last batch is a FACT, so no noise result is served in full — dropped noise 16/16 on every armed row, dropped facts 8 → 4 → 2, yielded 1 on both held rows, stand-downs 0 everywhere (a fact every 3rd leaves a noise turn between held facts, so no two consecutive removed-nothing visits; the stand-down is pinned by test, not by this bench); wire-tool-bytes 276 → 380 → 430, tool-msgs 6 → 10 → 12, receipt-msgs 13 → 21 → 25. The first table's `sliding+findings` row MOVED (2 → 6 facts verbatim, 41.5% → 32.9%, 376 → 474 bytes, 6 → 10 tool-msgs) because the hold is on by default under `.findings()` with a window; the other four rows are the step-3 print to the digit; the step-2 law still holds. The removable-set follow-up's condition (ordering among unpinned turns still matters after the collapse) did NOT hold — NOT taken; design page § Step 4 has the verbatim print and the reading.
- 2026-09-17 · step4 · FACT · docs: `npm run docs:truth` was EXIT 1 on the wiring tree (`LedgerFactPin` a NEW UNDOCUMENTED EXPORT) and is EXIT 0 (no regression, 0 contradictions) after naming it in prose on `docs-next/content/docs/build/agent.mdx` (new "Held in the window — `keepLedgerFacts`" subsection replacing the "accepted and inert" line: the hold, the four bounds, the two record keys, the dial, the bench numbers) and in `window-strategies.mdx` ("A declared fact stays too" section with the reference's real iteration-5 record, a refusal-engine bullet, a records paragraph, three export-table rows incl. `LedgerFactPin`). `compaction.mdx`'s `WindowRefusalReason` row calls the set closed and lists every member, so `ledger-fact` (9.102.0) was added there too — leaving it would have been the "doc text contradicts the code" class; its `WindowObservations` row now names `WindowRecord.ledgerFacts`. `findings-served.test.ts` §8 pins the word "inert" on the three SOURCE doc sites only (`types.ts`, `AgentBuilder.ts`, `Agent.ts`), not on any docs-next page, so the site prose is now true while the JSDoc still says inert — the owed one-packet fix (delete the five sentences, flip §8 to `not.toMatch`) stands; `registry.test.ts` reads only `agent.mdx`'s "typed events … domains" sentence, untouched. Both files green (24/24).
- 2026-09-17 · step4 · FACT · READMEs + record: `window/README.md` gained "The fact hold (9.102.0)" (the law, four bounds, refusal order, the pin/stand-down symbols) with the reference's REAL iteration-5 record as the example (first draft was hand-typed values — replaced; the `test/core/tools/reference/agent-findings-window.json` records are delta-encoded in the commit log, found by walking for `droppedStandings`) and `ledgerFactPins.ts` in the file list; `strategies/README.md` gained the "a strategy never reads the ledger" section with a consumer-strategy example that was COMPILED against `dist/types/index.d.ts` under `--strict` (scratch tsconfig with a `paths` mapping; the absolute-path import form resolves no declarations) — `WindowStrategy.plan` returns a `Promise`, so the example is `async plan`, and `WindowStrategyInput` has no `windowChars` (the example counts `history` itself); `findings/README.md` one paragraph "Held in the window (step 4)"; CHANGELOG `## [9.102.0] - 2026-09-17` (Added ×5 / Unchanged ×2, no names); design page § Step 4 (shape decision, rank rule, free-pin law + ceiling, stand-down family, what the record holds, named follow-ups incl. the "inert" packet and three hoists, bench + reading) and Track "4 eviction" ticked. Gates: `npx tsc --noEmit -p tsconfig.json` EXIT 0 after files 2 and 4; `npx eslint bench/findings-context.mjs` 0/0; `npm run bench:findings` exit 0; nothing committed, tmp/ and the 17 references untouched.
- 2026-09-17 · step4 · review 2 · finding 1 (three §8-pinned sites + the pin): `types.ts · AgentOptions.findings` block, `AgentBuilder.findings` and `Agent · findingsOptions` no longer say `keepLedgerFacts` is inert until the hold lands — each names where the resolved value is spent (`stages/window.ts · buildWindowStage` → `turns.ts · admitPins` as the `'ledger-fact'` pin ceiling, filed on `WindowRecord.ledgerFacts`). `test/core/agent/findings-served.test.ts` §8 was green only because the docs were false; it now pins the law instead of the waiting sentence: `not.toMatch(/keepLedgerFacts[^.]*\binert\b/)`, `not.toMatch(/until the hold lands|not yet acted on/i)`, `toMatch(/keepLedgerFacts[\s\S]*stages\/window\.ts/)` per site, header + test name rewritten. docs-next was already true (untouched). No record, plan, reference or bench byte changed: the 16 + `agent-findings-window` untouched, no build or bench re-run. `findings-served` + `window-ledger-fact` + `window-ledger-fact-pins`: 3 files / 57 tests passed; `tsc --noEmit` exit 0 after files 2 and 4.
- 2026-09-17 · step4 · review 2 · finding 2 (the top-level field): `types.ts · AgentOptions.keepLedgerFacts` "threaded to the stage and not yet acted on" replaced by the spend (the ceiling `turns.ts · admitPins` admits `'ledger-fact'` pins up to; the `limit` on the record's `ledgerFacts` block). That field was never a §8 site, so it is now the fourth — anchor `'\n  readonly keepLedgerFacts?: number | false;'`, newline-prefixed because `indexOf` on the two-space form lands on the nested four-space `findings.keepLedgerFacts` field first — and the nested field's own hover comment is pinned `inert`-free beside `serve`'s. Design page bullet "The "inert" sentences" rewritten as CLOSED with the lesson: a doc site that pins a "not yet" sentence must name the packet that flips it, or it is green on the day it lies.
- 2026-09-17 · step4 · FACT · the suite assumes `@anthropic-ai/sdk` is ABSENT (test/adapters/unit/AzureBrowserAndEnv.test.ts expects the Anthropic arm to throw) — the shuffle harness's `npm install --no-save @anthropic-ai/sdk` turned three provider-env tests red on the step-4 gate; the SDK is removed again after a real-model run (`npm uninstall --no-save`). Follow-up: make those tests independent of what node_modules holds. Also: the docs-next EXPORT build regenerates the tracked API-reference pages (623 files) — they are committed separately from the step.

- 2026-09-17 · packet6 · FACT · `reserved.ts · withFindingsArgument(schema, offer = [])` — the optional second parameter makes the two POINT-FREE call sites fail to compile (`seed.ts · scope.dynamicToolSchemas = deps.toolSchemas.map(withFindingsArgument)` and `buildToolsSlot.ts · merged.map(withFindingsArgument)`: TS2345, `.map` would hand the INDEX in as the offer). On purpose: the compiler names exactly the two sites the packet must revisit. Owed by the wiring stage (both files outside the findings-module stage's fence): `merged.map((s) => withFindingsArgument(s, offer))` at the one decoration site and `deps.toolSchemas.map((s) => withFindingsArgument(s))` at the seed fallback (no history → no offer). Until then `npx tsc --noEmit -p tsconfig.json` is red at those two lines only.
- 2026-09-17 · packet6 · FACT · `reserved.ts · withoutFindingsArgument` recognised the decoration by REFERENCE only (`properties._findings === FINDINGS_ARGUMENT_SCHEMA`), and the reference never holds on the live path: `callLLM · registeredToolSchemas` reads the committed `scope.dynamicToolSchemas`, a `structuredClone` of what the slot planted, so the choice seam's enum fence (`declaredEnumValuesOf(withoutFindingsArgument(schemaOf.get(c.name)))`) has been reading the schema WITH the decoration since 9.101.0 — verified by a throwaway armed run (both calls: `sameRef: false`, `peeled: false`; file deleted). Its unit test passed because it handed the function the live reference. Fixed in this file: `isFindingsDecoration` = the reference OR a plain object whose `description` opens with the versioned first sentence of `FINDINGS_DESCRIPTION` (`FINDINGS_MARKER`), so the base, an offer copy and a committed clone of either are all peeled; an author's own `_findings` (no marker) is still the same reference. Pinned in `test/core/agent/findings/reserved.test.ts` (a `structuredClone` of a decorated schema is peeled; an offer copy is peeled; an author's is not).
- 2026-09-17 · packet6 · FACT · Part C (bench/findings-shuffle.mjs) · `--matrix` / `AF_SHUFFLE_MATRIX=1` runs ONE model per invocation (`AF_SHUFFLE_MODEL`), so the brief's "one table per model" is two invocations; each prints one table per CELL (24: NOISE 2/4/8/16 × NOISE_AT end/start × NOISE_SIZE 250/1000/4000, noise outermost) plus one summary table (cell × condition → facts-in-answer, noise-cited, declared, standing-accuracy, drift). The cost line is printed BEFORE the first call for the RUNS you set — on the mock at RUNS=1: "cost: 1044 model calls for this model (nominal: n+1 per run, one per record read plus the answer; ceiling 1188 at maxIterations n+3), none made yet". `RUNS` defaults to 1 in matrix mode on the mock ONLY (env still wins); a real model keeps 5.
- 2026-09-17 · packet6 · FACT · `NOISE_SIZE` unset = the one-line noise record as before (about 15 tokens; the brief names 250|1000|4000 only), so `npm run bench:findings:shuffle` serves the same records, run-0 order `11 0 15 9 17 14 2 7 1 12 16 5 4 8 13 6 3 10` unchanged, and the default mock table prints the pre-packet numbers to the digit (facts-in-answer 1.000 ×3; noise-cited 1.000 / 0.600 / 0.600; declared - / 0.944 / 0.944; drift 0.20 / 0.80 / 0.80; unknown-id 0) plus the new column (standing-accuracy - / 1.000 / 1.000). The ONE default-mode plant change: every noise header line carries a near value (`pick-path latency <near>us`, `nearValue` = a fact ± 2..4, readings are 7 apart, so never equal and under 0.5%); it holds no `node-` so the mock's `readingIn` still reads it as noise. `assertDistinguishable` runs in EVERY mode before any call and refuses a plant where a noise byte holds a fact value or a node id, a near value equals a fact, or `score` reads an answer citing every near value as anything but 0 facts.
- 2026-09-17 · packet6 · FACT · `standing-accuracy` is POOLED (agreements / named results over all runs), read on the LAST standing row per id (`foldLedger`'s rule, `standingsAgainstTruth`), not a mean of per-run ratios; a known id the paging tool served no record under (an exhausted-cursor reply) is left out of the denominator; `-` when nothing named a known id. The truth map is the paging tool's own `served` map keyed by `ToolExecutionContext.toolCallId` (the id the tool message carries) — no history scan, no inference. Verified: 1.000 on every armed row of every cell on the mock, i.e. the ids the tool filed ARE the ids the ledger named (the smoke pins it: armed = 1, off = `-`).
- 2026-09-17 · packet6 · FACT · `NOISE_AT` is applied AFTER the seeded shuffle as a stable partition (`orderFor`: `spread` = the untouched shuffle and the default; `end` = every noise index after every fact index; `start` = before; each group in the order the shuffle gave it). The smoke laws are COMPUTED from the orders, so they held unchanged: under `end` every order ends on noise → armed rows cite noise 1.000 and drift = distinct last SKUs / runs (0.80 at 5 runs on the default plant); under `start` 0.000 / 0.20. Padding (`paddingRows`) = stock-movement rows of the SAME SKU seeded by the record index alone (the same bytes every run and seed), sized at 4 chars/token, stated in the header ("about 250 tokens each at 4 chars/token (17 stock-movement rows, 1051 chars per record; …)"; 4000 → 278 rows, 16031 chars).
- 2026-09-17 · packet6 · FACT · the agent's default `contextBudget` (messages 10000 chars) warned "messages slot over budget … Nothing was truncated" 15 times on ONE padded single-cell run at NOISE_SIZE=250; `budgetFor` now sizes the three slots from the plant per run (messages 10000 + 2 × plant chars + 400 × n; systemPrompt 4000 + 300 × n; tools 6000 + 64 × n — the option's own doc: a signal, never a limiter, raise it when the slot is legitimately that big) → 0 warnings across the whole mock matrix and the largest single cell (NOISE=16 NOISE_AT=end NOISE_SIZE=4000, 5 runs, green). Mock matrix wall time 41.2 s (24 cells × 3 conditions × 1 run, the 4000-token cells included); eslint on the file 0/0.
- 2026-09-17 · packet6 · FACT · the harness ran against the dist built from the CLEAN tree at stage start (`npm run build` exit 0, first); the sibling stages' edits to `findings/reserved.ts`, `serve.ts`, `types.ts` and the new `offer.ts` were in progress in the same checkout and are NOT in that dist. The mock names `last.toolCallId` — an id any offer would list — so the smoke is expected to stay green under Part A; re-run `npm run bench:findings:shuffle` and `AF_SHUFFLE_MATRIX=1 npm run bench:findings:shuffle` after the next build. NOT touched, owed elsewhere: the CHANGELOG line for the harness (second `### Added` block under `## [9.102.0]`, package.json is still 9.101.1), the real-model page's matrix tables (a hosted run — no key in this stage), and any docs mention of the new axes/columns.
- 2026-09-17 · packet6 · FACT · the compile error above is LIVE under vitest (no typecheck at run): the point-free `seed.ts · .map(withFindingsArgument)` handed the index in as the offer and every armed run crashed at seed (`offer.slice is not a function` — 51 failures across 7 neighbouring files on the first run, one cause). Since a sibling stage shares this checkout, `withFindingsArgument` now treats a NON-ARRAY offer as no offer at runtime (`Array.isArray(offer) ? offer : []` — the base decoration by reference, byte-identical to 9.102.0); the compile-time refusal (TS2345 at both sites) is unchanged and remains the wiring stage's signal. Pinned in `reserved.test.ts` ("passed point-free to `.map` … the base by reference, never a crash").
- 2026-09-17 · packet6 · FACT · `servedToolCallIds` MOVED from `serve.ts` to `offer.ts` (the leaf; `serve.ts` re-exports it by name so `callLLM`, `servedView` and the tests keep their import path — `offer.test.ts` pins the re-export as the same reference). Reason: `undeclaredResultIds(messages, rows)` needs the wire's ids and `serve.ts` needs the undeclared set — keeping the ids in `serve.ts` would have made `offer.ts ↔ serve.ts` a cycle. The shared set is `offer.ts · undeclaredIds(served, standingOf)` (wire order, each once — the former module-private `serve.ts · undeclaredOf`, byte-identical output, pinned by exact strings in `serve.test.ts`); the offer is that list reversed.
- 2026-09-17 · packet6 · FACT · the brief's "quotes the DECLARING call's proposition" is read as the proposition declared ON THE JUDGED CALL ITSELF (the basis row whose `toolCallId` is the result being stood on — `_findings.proposition` is written before that call's result exists, which is the whole point: `serve.ts · propositionsOf(rows).get(row.toolCallId)`), not the proposition of the later call that carries `previous[]`. Pinned: "it is the judged call's own proposition, not the declaring call's". The mark "as the model's" is the bucket heading every such line already sits under ("… (declared by the model):") plus the label `tested:`; no extra token per line (the cache law — bytes move only with the ledger). Grammar as the brief wrote it: `ruled out (<tool>, tool:<id>): <line> — tested: <proposition>`; the open form appends the same suffix after ` · settles: …` on every assertion line; `nextSteps` and fact lines never carry it; `predicts` is record-only.
- 2026-09-17 · packet6 · FACT · `PROPOSITION_CHARS` (= 240) lives in `findings/types.ts` beside the vocabularies and the clip in `ledger.ts · clipText` (NOT `serve.ts · FINDINGS_PIECE_LIMITS.lineChars`: `serve.ts` imports `ledger.ts`, so the row's cap cannot be read from the piece's limits without an inversion). The row clip states the cut (`…[clipped N chars]`) and does NOT fold newlines — the record keeps the model's text; the piece's `clipLine` folds when it quotes. `LedgerFold` is untouched: the proposition lookup is a local walk in `serve.ts` (`propositionsOf`), so `stages/window.ts`'s fold consumer sees no shape change.
- 2026-09-17 · packet6 · FACT · no root export was added by this stage (`src/index.ts` is outside its fence): `FINDINGS_OFFER_CAP`, `undeclaredResultIds`, `undeclaredIds`, `PROPOSITION_CHARS` are module-path only. `FindingsDeclaration` / `BasisRow` (already root exports) gained the two optional string fields, and `FindingsDeclaredPayload` gained `hasProposition?: true` — additive, no ratchet moved. The wiring/docs stage decides whether `FINDINGS_OFFER_CAP` / `undeclaredResultIds` are named at the root (the brief's "every new root export named in prose").
- 2026-09-17 · packet6 · FACT · the instruction is 10 lines now (was 8): line 3 asks for `proposition` / `predicts` before an exploratory call; the last line asks for the id "exactly as it appears in the tool schema's list for `previous[].toolCallId` … never a position or a count" (the answer-turn sentence). The schema's top description gained one clause naming the two fields; `proposition` / `predicts` sit between `expect` and `previous`. All of it passes `unprovable` at `persistent-history` (the strictest lifetime) for the base, an offered copy and a clipped offered copy (`reserved.test.ts` walks every `description`); the first wording of the proposition field said "what this call tests" and would have tripped the bare-call-deictic row — it is "what the call tests".
- 2026-09-17 · packet6 · FACT · `bench/findings-shuffle.mjs` shows as modified in this checkout and was NOT touched by this stage (Part C's file) — left alone.
- 2026-09-17 · packet6 · FACT · gate on the findings-module stage: `npx tsc --noEmit -p tsconfig.json` red at the two point-free call sites ONLY (`seed.ts:435`, `buildToolsSlot.ts:817` — the wiring stage's lambdas); `npm run test:types` the same two lines; eslint 0 errors on the 11 touched files (1 pre-existing warning, `reserved.ts · withoutFindingsArgument`'s unused `_decoration`). vitest: `test/core/agent/findings/` (4 files) + `test/adapters/reservedArgumentSurvives.test.ts` green; the neighbouring run (33 files: modelFacingSurfaces, modelFacingScan, findings-ledger, findings-served, findings-declarations, unsupportedArgument, tool-args-validation, receipt-conformance, byte-identity, window-ledger-fact, window-refusal-rename, test/events) = 571 passed / 2 failed, the two ARMED byte-identity references `agent-findings` and `agent-findings-window` — values only: the schema's top description (one clause), the two new `proposition` / `predicts` properties, and the instruction's two new lines at every site that records it; the 16 unarmed references pass untouched. Regenerating those two ALONE is the packet's byte-identity step (the enum joins their delta once the offer is wired). Owed elsewhere once the offer is wired: `receipt-conformance.test.ts` ("the registry schema, decorated by the same function, is the served schema") hashes `withFindingsArgument(undecorated)` with NO offer — at epochs ≥ 2 the served schema carries the offer, so that comparison must decorate with the same offer.
- 2026-09-17 · packet6 · FACT · wiring stage: the tools slot CANNOT reach `history` — `buildToolsSlot` is an isolated subflow seeded only by the Tools branch's `inputMapper` (both charts), so the offer crosses the mount as the per-iteration arg `findingsOffer = undeclaredResultIds(parent.history, parent.findingsLedger)`, computed in the branch's `inputMapper` in `buildAgentChart` AND `buildDynamicAgentChart` (the duplicated-mapper house pattern) under a new chart dep `AgentChartDeps.hasFindingsLedger` (the `hasSteps` / `hasEscalation` grammar; `Agent.buildChart` threads it from `findingsOptions`, the same value that arms the slot and `callLLM`). The slot reads `args.findingsOffer` under `config.findings === true` only and decorates `merged.map((s) => withFindingsArgument(s, offer))` at the ONE site; `seed.ts` decorates with no offer (`.map((s) => withFindingsArgument(s))`). Chosen over mapping `history` + `findingsLedger` into the slot: the mount already reads both, and seeding the whole history into a second subflow every armed iteration would double what the messages subflow's seed already records.
- 2026-09-17 · packet6 · FACT · `parent.history` at the Tools mount IS the served list on both shapes: the window stage (`STAGE_IDS.COMPACT`) runs before the Context fan-out on the flat chart and on the OUTER chart before the `sf-llm-call` boundary on the grouped one (inside, `history` is `dynamicTurnSeed`'s copy of `priorHistory`; `findingsLedger` is the boundary's value-conditional input from step 3). `callLLM` reads the same `history`; `collapseJudged` never changes an id; so the offer, the piece's `undeclared:` line and the wire agree — pinned end to end by `findings-ledger.test.ts` § 13 (a `slidingWindow({ keepRecentTurns: 2 })` run: the offer is the wire's undeclared ids reversed; an evicted id is not on it).
- 2026-09-17 · packet6 · FACT · the log moves where it must: with the offer changing per call, the Tools mount's merge-back now WRITES `dynamicToolSchemas` on every epoch after the first (one `overwrite` entry + one `set` trace row per epoch) where 9.101.0/9.102.0 recorded an EMPTY commit — an unchanged decorated list was a no-op `arrayMerge: Replace`. That is the byte-identity delta's second family (test header); the served views' `messages`, tickets, ledger rows, window records and gaps did not move on either armed reference.
- 2026-09-17 · packet6 · FACT · `receipt-conformance.test.ts` (armed section) now derives the offer FROM THE RECORD — `undeclaredResultIds(view.messages.asSent, ledger.filter(row => row.iteration < view.epoch))` (a row's `iteration` is its declaring call's epoch, so the rows present at the call of epoch k are those with `iteration < k`) — and pins `schemaHashes[name] === hash(withFindingsArgument(undecorated, offer))` on every epoch plus `!== hash(withFindingsArgument(undecorated))` once an offer is served; the expected offers by the script are `{1: [], 2: ['c1'], 3: ['c2']}`.
- 2026-09-17 · packet6 · FACT · version string: the packet rides the UNRELEASED `## [9.102.0]` entry (package.json is 9.101.1 — a second `### Added` block + `### Unchanged` added under it), so every comment this stage wrote says 9.102.0. The findings-module stage's `serve.ts` (line comment above `export { servedToolCallIds }`) still says "since the offer (9.103.0)" — outside this stage's fence, owed to whoever next touches `serve.ts`.
- 2026-09-17 · packet6 · FACT · no root export added (`src/index.ts` untouched): `FINDINGS_OFFER_CAP` / `undeclaredResultIds` stay module-path; the served view's `tools.schemas` carries the offer and the clipped description states the cap. `docs:truth` therefore has nothing new to ratchet; the site page names only root exports (`FindingsDeclaration`, `BasisRow`, `servedAt`, the event payload flag).
- 2026-09-17 · packet6 · FACT · gate on the wiring stage: `npx tsc --noEmit -p tsconfig.json` exit 0 (was red at the two point-free sites); `npm run test:types` exit 0; eslint 0 errors on the 9 touched files (8 pre-existing warnings, all non-null assertions / one `_kind`); `npm run build` exit 0; vitest — findings-ledger 34/34 (4 new), findings-served + receipt-conformance 91/91 (3 new), byte-identity 18/18 after the two armed regenerations (16/16 unarmed untouched before them); `npm run bench:findings:shuffle` on the fresh dist prints the pre-packet default-mode row to the digit (facts-in-answer 1.000 ×3; noise-cited 1.000 / 0.600 / 0.600; declared - / 0.944 / 0.944; standing-accuracy - / 1.000 / 1.000; drift 0.20 / 0.80 / 0.80; unknown-id 0) and the smoke is green; `npm run bench:findings` prints the step-4 tables to the digit, both laws green. No hosted run (no key).
- 2026-09-17 · packet6 · FACT · docs gate on the wiring stage: `npm run docs:truth` "no regression against the baseline" (0 promises of non-existent things, 0 contradictions; no new root export, so nothing to ratchet); `cd docs-next && EXPORT=true npm run build && npm run check:site-budget` → "Site budget passed" (deferred demo 420.5 KB gzip, search 1.93 MB gzip). The site build's `prebuild` (`scripts/gen-fumadocs-api.mjs`) regenerated 8 TypeDoc pages under `docs-next/content/docs/api/` (`BasisRow`, `FindingsDeclaration`, `StandingRow`, `ConflictRow`, `ConflictWitness`, `LedgerFactPin`, `FindingsLedger`, `FindingsRow` — the leaf's two new fields and the line shifts in `findings/types.ts`); they are generated artefacts this repo commits (the `accd7c4e` precedent) and were not hand-edited. Full suite: `npx vitest run` 666 files passed / 1 skipped, 11215 tests passed / 20 skipped, 38.5 s. Prettier: three files (`buildToolsSlot.ts`, `findings-ledger.test.ts`, `findings-served.test.ts`) reformatted by `prettier --write`, `--list-different` clean on the nine touched TypeScript files after.
- 2026-09-17 · packet6 · review 2 · BLOCKING (offer vs batch) FIXED · the offer listed every undeclared id on the served history while `standingRowsFrom` resolved against `scope.toolResults` — the last batch, reset by `toolCalls.ts` every iteration — so an offered id copied from an earlier batch filed `unknownId: true` with no `toolName` (the reviewer's `offer-vs-batch.mjs` reproduced it on dist: call-3 offer `['c2','c1']`, c1 filed unknown, yet collapsed on the wire by id). Root fix, one identity source: `findings/offer.ts · knownResults(messages, previousBatch)` — the previous batch first, then every served `role: 'tool'` message read as a `PreviousResult` (`toolName`, `content`; a placed ticket still resolves to `artifact:<ref>`), each id once — passed by BOTH doors (`toolCalls.ts` at the batch read, `route.ts` on the answer turn) from `scope.history`, the SAME list the Tools mount read the offer from. `PreviousResult.toolName` is optional now (a served message with no name yields a row with none — nothing invented; `standingRowsFrom` spreads it only when present). Pinned: `findings-ledger.test.ts` § 13 "EVERY id the offer lists resolves" (a mock that names every offered id at every call and on the JSON answer: six rows, all `toolName: 'look'`, no `unknownId`, on `dynamic` and `dynamic-grouped`), `offer.test.ts` "THE LAW AT THE ROW" (+ the 300-run property with no batch at all), `ledger.test.ts` (a known result without a name). The probe now prints the c1 row resolved (`toolName: 'look'`, no `unknownId`). FENCE: `src/core/agent/stages/toolCalls.ts` and `src/core/agent/stages/route.ts` are OUTSIDE packet 6's touched set and were edited here (one expression each: the first argument of `standingRowsFrom` + one import) — the two only call sites of the resolver; no other way to reach the blocking fix from inside the fence.
- 2026-09-17 · packet6 · review 2 · MAJOR (classic) FIXED · `reactMode: 'classic'` selects the Tools branch on turn 1 only, so `dynamicToolSchemas` stayed at the offer-less base for the whole run (the reviewer's `classic-offer.mjs`: `classic ["NO-ENUM"×4]`) and the docs' "from the second call on" was false for a public mode. Decided: the named refusal (the `AgentBuilder.selfExplain` / `.skillGraph()` / `steps` house pattern), placed in the `Agent` CONSTRUCTOR beside `keepLedgerFacts`'s resolution (inside the fence — `AgentBuilder.ts` is not; the builder passes `findings: this.findingsValue` into the constructor, so both doors hit it): "Agent: .findings() requires per-iteration slot recomposition — reactMode 'classic' caches the tools slot on turn 1 …". Pinned in `findings-ledger.test.ts` § 7 (both doors throw; `dynamic-grouped` builds; classic without `.findings()` builds). Recorded as `### Changed` in the CHANGELOG (9.101.x accepted the combination and degraded it silently), on the design page § Packet 6 "Classic is refused", in `build/agent.mdx` and the findings README. The alternative — selecting the Tools branch every turn on an armed classic agent — would change what "classic" means for one arm; not taken.
- 2026-09-17 · packet6 · review 2 · MAJOR (revision) FIXED · a declared id left the offer on ANY standing, and the enum forbids naming an unlisted result, so `open` → `fact` and `fact` → `ruled-out` were unreachable although the fold's last-wins law and the conflict witness's retirement depend on them. Decided for the library: the offer is what the model can still READ — no standing, `fact` (its assertions in the piece under every mode, verbatim under the default) and `open` (verbatim, carrying `settles`) stay listed; `noise` and `ruled-out` leave (`offer.ts · RETIRING_STANDINGS`: a ticket on the wire under every mode, a count or one line in the piece, nothing left to re-judge — a wrong `ruled-out` is answered by a NEW call). `undeclaredResultIds` RENAMED `offeredResultIds` (the old name would have lied: the offer now holds declared ids); `nameableIds` is the wire-order twin; `undeclaredIds` (the piece's line) is unchanged and is now a SUBSET of the offer, not its reverse — the "one set" sentence became "one owner, two sets" in `offer.ts`, `serve.ts`'s header untouched (it still reads `undeclaredIds`). The instruction's ninth line: "Never restate a result in a standing. Name a result again only to change its standing; a result you never name has no standing — leave it unnamed rather than guess." (passes `unprovable` at every surface — `reserved.test.ts`, `modelFacingSurfaces.test.ts` green); the cap sentence says "newest results you may still name". Costs named on the design page § "Revision": a held fact stays in the offer until the cap cuts it; a checklist-reading model may restate facts (one more row per restatement, the fold collapses it). Expectations moved: `findings-ledger.test.ts` § 13 call 3 = `['c2','c1']`, answer = `['c3','c2','c1']` (+ a "retired result leaves" case); `serve.test.ts` offer over the hand-built ledger = `['call_x','call_8','call_4','call_3','call_2','call_1']`; `receipt-conformance.test.ts` unchanged in value (its c1 is declared NOISE) beyond the rename; `offer.test.ts` rewritten around the new laws (revision reachable in both directions, the subset relation, the property).
- 2026-09-17 · packet6 · review 2 · MAJOR (CHANGELOG Fixed) FIXED · a `### Fixed — the choice seam's enum fence read the decorated schema` block now sits under `## [9.102.0]`: since 9.101.0 `withoutFindingsArgument` recognised the decoration by reference only and the live list is a `structuredClone`, so `declaredEnumValuesOf` judged arguments against the schema WITH `_findings` and a value equal to a findings vocabulary word was excused; recognised by the versioned marker now; an armed agent may file an `unsupported-argument` finding it previously suppressed; pinned by `reserved.test.ts` (the clone case, from the findings-module stage).
- 2026-09-17 · packet6 · review 2 · MAJOR (tools cache) NAMED, not measured · from the second call on an armed agent's tool schemas vary per call, so a `field: 'tools'` cache breakpoint (`cache/types.ts`; `AnthropicCacheStrategy` marks system/tools/messages; `CacheDecisionSubflow` pushes a `'tools'` slot when an injection contributes tools) cannot hit on such a run and on a prefix-cached wire every breakpoint after it misses too; step 3 already recorded the system block moving on every declaring call — the offer moves the tools prefix AHEAD of it. No bench counts cache tokens, so no number is quoted (the step-3 note stands: a real-provider run reading `usage.cacheRead`/`cacheWrite` per epoch). The lever not taken — the offer in the request-only system piece, below the tools breakpoint — is named on the design page § "The cache" with why (the piece is prose; prose is what failed on the real model), and a sentence in `build/agent.mdx` and the CHANGELOG.
- 2026-09-17 · packet6 · review 2 · references · `agent-findings` and `agent-findings-window` regenerated each ALONE (`-t 'agent-findings$'` / `-t 'agent-findings-window$'` under `AF_TOOLS_REFERENCE=update`; the 15 other reference files copied aside first and `cmp`-equal after; `git status` shows only the two). Structural diff against the copies: `agent-findings` 28 moved paths, the window scenario 104, in five families and NO other — the enum keeping a declared fact (`c1` at `agent-findings` epoch 3; `c1` from epoch 3 and `c3` from epoch 5 in the window scenario: 2 and 24 enum paths), the instruction's revised line at every recording site (+ `contentHash`), `receipt.system.chars`/`hash`, the four `requestMeasurement` numbers per call-llm commit. No message, ticket, ledger row, window record or gap moved. Header updated on `byte-identity.test.ts`.
- 2026-09-17 · packet6 · review 2 · benches · built first (`npm run build` exit 0), then sequentially with nothing else running: `npm run bench:findings` prints the step-4 tables TO THE DIGIT (both laws green — the offer and the instruction line are not in its columns); `npm run bench:findings:shuffle` prints the default-mode row to the digit (facts-in-answer 1.000 ×3; noise-cited 1.000 / 0.600 / 0.600; declared - / 0.944 / 0.944; standing-accuracy - / 1.000 / 1.000; drift 0.20 / 0.80 / 0.80; unknown-id 0), smoke green — the mock names `last.toolCallId`, which every offer lists under either rule. No hosted run (no key); the real-model page's pre-offer tables stand and its "What changed" paragraph now states the revised rule, the row-level law and the classic refusal.
- 2026-09-17 · packet6 · review 2 · gate · `npx tsc --noEmit -p tsconfig.json` exit 0 (after every second file, three runs); `npm run test:types` exit 0; eslint 0 errors on the 17 touched TypeScript files (10 pre-existing warnings, all on lines this round did not write); prettier `--list-different` clean on the touched set after `--write` on seven of them (the six NUL bytes in `serve.test.ts` are `assertionKey`'s separator written literally, present in HEAD — preserved); `npm run build` exit 0 BEFORE the benches and the probes; full `npx vitest run` on the finished tree: 666 files passed / 1 skipped, 11229 tests passed / 20 skipped, exit 0 — two rounds earlier it was red on `release-gate-interlock` (the committed `docs/DOCS_TRUTH_REPORT.md` was stale: the previous stage ran `docs:truth` but never `docs:truth:report`; regenerated LAST this round — `agentfootprint.findings.declared` now lists `build/agent.mdx`, 587 → 588 site code blocks) and on `modelFacingScan` (the classic refusal is one more host-facing literal in `Agent.ts`: census row 7 → 8, `notModelFacing` 119 → 120, `total` 180 → 181, the reason beside the numbers); `npm run docs:truth` "no regression against the baseline" (0 contradictions); `cd docs-next && EXPORT=true npm run build && npm run check:site-budget` → "Site budget passed" (the prebuild regenerated 9 API pages under `docs-next/content/docs/api/`, the committed-artefact precedent, not hand-edited). Reviewer probes on the fresh dist: `offer-vs-batch.mjs` → offers `["NO-ENUM",["c1"],["c2","c1"],["c3","c2","c1"]]` and the c1 row `toolName: 'look'`, no `unknownId`; classic → "REFUSED" through both doors, `dynamic` / `dynamic-grouped` build.
- 2026-09-17 · packet6 · hosted matrix pass 1 (Haiku 4.5, 8 cells × 3 conditions × 3 runs, 1,224 calls stated): 4 cells completed before the Anthropic account's credit ran out; unknown-id standings 0 everywhere (the offer binds), `declared` 0.00–0.53, standing-accuracy 0.44–1.00; tables on the real-model page; the SDK removed again after the run. The harness gained env cell filters (MATRIX_NOISE / MATRIX_AT / MATRIX_SIZE) because the full 24-cell matrix (3,132 calls) is not what a first pass should spend. Next: credits, then the 16-noise cells and the Sonnet pass.
- 2026-09-17 · packet6 · hosted matrix pass 2 (Haiku 7/8 cells, Sonnet 5/8; credit ran out again): unknown-id 0 everywhere; Sonnet declares 0.27–0.97, Haiku 0.00–0.60; standing accuracy 0.61–1.00; the recency-plus-mass cell (noise 4, at end, 1000 tokens) shows noise cited 1.000 without the ledger vs 0.000 with ledger-and-facts on Sonnet (3 runs); ledger-only loses facts (0.667 at noise 16) — stays gated, default unchanged; facts-in-answer dips to 0.944 in ledger cells. Tables on the real-model page. SDK removed after the run.
- 2026-09-17 · packet6 · signal cell at RUNS=10 (noise 4 · at end · 1000, both models, 330 calls each): the three-run signal did NOT hold — Sonnet noise-cited 0.200 off vs 0.200 ledger-and-facts (0.400 ledger-only); Haiku 0.100 vs 0.000; facts-in-answer drops to 0.917 under the ledger on both models. Recorded as a null result with the suspected mechanism (a wrong `fact` standing is served in the piece — the model's error made sticky). Next: instruction-variant bench on the fidelity dip; heavy cells only on a stated budget. SDK removed.

- 2026-09-17 · packet7 · the answer-turn ask as a dial · `findings({ answerAsk: 'none' | 'quote-facts' })` (default `'none'`, bench-gated): under `'quote-facts'` `serve.ts · findingsLedgerPiece(rows, served, answerAsk)` ends the piece with a blank line and `reserved.ts · FINDINGS_ANSWER_ASK` (five lines — copy each fact value as written; `evidenceRefs`/`nextSteps` unsettled, say so; a `{"collapsed":true,…}` ticket judged noise/ruled-out, do not draw on it; undeclared served in full, may be used; never invent a value — passes `unprovable` at persistent-history; a PRODUCERS row in `modelFacingSurfaces.test.ts`; the scan flagged nothing, so the census has no new row). Threaded beside `findingsServe` everywhere it goes — `Agent.ts` (seed deps + call-llm deps, VALUE-conditional inside the arm: only `'quote-facts'` is threaded), `seed.ts` (run constant `AgentState.findingsAnswerAsk?: 'quote-facts'`, written only when threaded), `callLLM.ts` (`deps.findingsAnswerAsk ?? 'none'`), `servedView.ts · answerAskOf(readRunConstant(location, 'findingsAnswerAsk'))` — so an armed agent on the default commits 9.102.0's key set and the rebuild is byte-equal by construction. `AgentBuilder.findings` refuses any other value; stores `answerAsk` as given (no normalisation — `'none'` and absent reach the run as the same thing). Bench: fourth condition `ledger+ask` = `.findings({ serve: 'ledger-and-facts', answerAsk: 'quote-facts' })` after `ledger-only`; on the mock the row equals `ledger-and-facts` to the digit (the echo ignores prose — header says so); smoke laws unchanged. Tests: `serve.test.ts` (+6), `findings-served.test.ts` (+6, both shapes), `findings-declarations.test.ts` (+7), `receipt-conformance.test.ts` (+2, both shapes); the 18 byte-identity references pass untouched. Docs: findings README § "The answer-turn ask (packet 7)", `build/agent.mdx` one paragraph, CHANGELOG `## [9.103.0]` `### Added`. The prompt's signature sketch `(rows, iteration, served, answerAsk)` was not followed: the piece has carried no iteration since the cache law (step 3 review) — the real signature is `(rows, served, answerAsk = 'none')`.
- 2026-09-17 · packet7 · review (Sonnet): the ask's collapsed-ticket line said 'judged noise or ruled out' — false under `serve: 'ledger-only'`, where a fact collapses too; reworded mode-agnostically ('carries no data: judged noise or ruled out, or its fact is already listed above'); pinned by the surfaces row.
- 2026-09-17 · packet7 · hosted run, the ask at RUNS=10 (noise 4 · at end · 1000; 440 calls per model; the first Haiku attempt died on `[anthropic] terminated`, rerun): with the ask facts-in-answer 0.950 (Sonnet, from 0.883) / 1.000 (Haiku), noise-cited 0.100 vs pile 0.300 (Sonnet), 0.000 vs 0.100 (Haiku), drift down; ledger-only worst again. DECIDED: `quote-facts` becomes the armed default in the next minor ('none' = opt-out). Harness follow-up: a per-run retry on a dropped stream. SDK removed.
- 2026-09-17 · classifier · Part A · `src/classify/{types,typesafe,mock,index}.ts` + the door `src/doors/classify.ts` → `agentfootprint/classify` (package.json `exports` + `typesVersions`; `subpath-exports.test.ts` SURVIVING_SUBPATHS + a same-object section; `door-aliases.test.ts` DOORS — the fourteenth). The brief named `src/classify.ts` as the door; the code's shape for every door since 9.34.0 is `src/doors/<name>.ts` over an implementation barrel, so the door is `src/doors/classify.ts` re-exporting `src/classify/index.ts` (the `./providers` / `./maps` shape). The brief's "tsconfig paths" has no counterpart: `tsconfig.json` `paths` maps footprintjs subpaths only, no door is listed there, nothing added. `typesafe()` takes `retryDelayMs` (default 250) beyond the brief's five options so a backoff test is not a slow test; abort during a wait rejects with the caller's reason, unwrapped. Tests `test/classify/{typesafe,mock}.test.ts` (21): request bytes, auth header, URL, alias, the probe mapped field for field, no renormalisation (a distribution summing to 2 kept), score + legend, usage half-present dropped, unknown answer type refused by name, 429 then success with latency spanning the wait, 529 × maxRetries, 401/422 final, key absent from the adapter's own error text, abort before and during backoff.
- 2026-09-17 · classifier · Part B · `JudgmentRow` / `JudgmentErrorRow` on `findings/types.ts` (+ `JUDGE_RESULT_CHARS` = 4000), `foldLedger(...).judgments` (last wins; `standingOf` untouched), `findings/judge.ts` (`judgeQuestions` pure; `judgeResult` the async writer through `recordFindings`; `STANDING_CRITERIA`, `JUDGE_QUESTION_IDS`), `AgentOptions.findings.judge` validated in `AgentBuilder.findings` (must be `{ name: string; classify: fn }`), threaded VALUE-conditionally as `ToolCallsHandlerDeps.findingsJudge` (`Agent.ts`), called by `toolCalls.ts · judgeLanded` at the FIVE places a result joins the batch — the brief said "the one place"; the code has the execute loop's `batchResults.push` + `scope.toolResults = [...]` and four resume doors through `appendBatchResult` (ask, decision, consent, pause), each awaited right after the entry exists and before `tool_end`. Events `agentfootprint.findings.judged` / `.judge_failed` — the brief's `judgeFailed` is refused by the registry's own form test (`^agentfootprint\.[a-z_]+\.[a-z_]+$`), so snake_case; registry, `ALL_EVENT_TYPES` 113 → 115, the eight doc counts, MENTAL_MODEL's domain line, observability.mdx prose. The probe in the brief carried the RESPONSE only, no criteria text, so `STANDING_CRITERIA` is written in the ledger's own words (one line per standing). The brief's `tests_proposition` id is `tests_subject` on the wire the library sends (the subject may be the question; the probe's own answer id is quoted as-is in the fixture). Tests: `findings/judge.test.ts` (13), `findings-judge.test.ts` (8, on the real loop: the provider snapshots the judge's call count → `[0, 1, 2]`; row order basis·judgment·standing·basis·judgment; policy A pinned on the wire), `findings-events.test.ts` (+3), `byte-identity.test.ts`: the tree holds 17 references (the brief said 18), all 17 untouched (run first, copied aside, `cmp`-equal after); `agent-findings-judge` generated alone — 46 added / 15 moved (index shifts) / 0 removed against `agent-findings`, 0 under `served.*`.
- 2026-09-17 · classifier · Part C · `src/lib/injection-engine/classifierScorer.ts` (PURE_CORE in the fence; `src/classify/types.ts` a PURE_LEAF), exported beside `keywordScorer` on `agentfootprint/skill-graph` and the injection-engine barrel with `CLASSIFIER_SCORER_QUESTION` ('entry') and `CLASSIFIER_SCORER_UNAVAILABLE`; `Classifier` re-exported as a type from the skill-graph door. `rankEntries` is NOT used: there is no raw score to soften — `score` = `relevance` = the probability as sent; `chosen` = the provider's `choice` (a pick outside the candidates → no chosen; an unscored candidate → 0, not a share). `pickEntry.ts` needed no change: an undefined `chosen` already leaves the cursor unset for the cold-start pick (pinned on the real loop, `entryScorer` = 'classifier:unavailable', `entryScores` = []). Tests `classifierScorer.test.ts` (11).
- 2026-09-17 · classifier · Part D · bench `AF_SHUFFLE_JUDGE=mock|typesafe` (four columns printed only when set; smoke laws under mock: 1 / 1 / tokens > 0 / 0 errors on armed rows, `-` on off; the cost line names the classifier calls: 270 on the default cell); `AF_SHUFFLE_JUDGE` unset prints the 9.103.0 table byte for byte (both prints in the scratchpad, mock print pasted on the design page). Docs: `src/classify/README.md`, findings README § The judge, scored-choice page (gate opened; steps 1–2 landed; the capability is a port, not `LLMProvider.scoreChoices?`; the fallback is the empty ranking), findings-ledger page § Judge (first fact, policies A–D, the bench, what is pinned), `build/agent.mdx` § A second source (every new root/door export named in prose), CHANGELOG `## [9.104.0] - 2026-09-17`. No hosted classifier call was made; no commit.
- 2026-09-17 · classifier · gate · `cd docs-next && EXPORT=true npm run build && npm run check:site-budget` went RED by 0.2 KB (`deferred demo async payload: 422.2 KB exceeds 422.0 KB`) with `toolCalls.ts` importing `findings/judge.ts` statically — the judge family walked onto the default graph. Fixed by the budget's own law (an optional family reaches the bundle through `import()` where its option is enabled, and dispatch is already async): `toolCalls.ts · judgeLanded` now `await import('../findings/judge.js')` (the `selfExplain.ts · lazilyMountedTraceTools` precedent); an agent without a judge never loads the module. Re-measured after the change (number in the report). Other gate fixes found by the full suite: `src/classify/README.md` opens with a role word (`Support`); CLAUDE.md's doors line says SIXTEEN and lists `/classify` (`documented-doors.test.ts` pinned to 16); a capability-index row (its `where` cell must be ONE path); the gitignored local SKILL.md install re-copied (the 113 → 115 count).
- 2026-09-17 · classifier · review (Sonnet): the adapter quoted a provider error body verbatim, so a body echoing the credential would have carried the key into a log, and the test named the law without asserting it — the key (whole or any 12-char run) is now redacted before the quote, pinned by the 401 case.

- 2026-09-17 · classifier · review · MAJOR `agrees` was dead by construction: `ledger.ts · emitRow` compared the judgment with `standingOf` at judgment time, but the judge files BEFORE the model call that declares, so no standing ever existed there. Moved to the one moment both readings exist — `findings.standing` carries `agrees?: boolean` (`fold.judgments.get(toolCallId)` vs the row's standing), present exactly when a judgment row exists; REMOVED from `FindingsJudgedPayload` outright (the "model declared first" case cannot happen: a result this run landed is judged before the next call, a served-history result never has a judgment). Pinned: `findings-judge.test.ts` (ruled-out vs noise → `agrees: false` on the standing event; a match → `true`; the judged event has none; absent after a failed judgment and on an unarmed run) and `findings/judge.test.ts` (the standing event after a judgment, never on a row); `findings-events.test.ts` key sets moved. payloads.ts, findings README, CHANGELOG, docs-next `agent.mdx` / `observability.mdx`, design page updated.
- 2026-09-17 · classifier · review · MAJOR denied / refused calls were judged: the execute loop's `judgeLanded` ran on a permission-denied / halted / fail-closed result and the resume doors regardless of `dispatched.executed`. `toolCalls.ts · judgeLanded` takes `ran`: the execute loop passes the nudge's own predicate (`executed && !denied && !skillRejected`), the three `dispatched` doors `dispatched.executed === true` (a local `toolRan` where `dispatched` is scoped inside the approved branch), the `pauseHere` door `true` (the person's answer IS the result by contract). A denial text is not a tool result. Pinned: `findings-judge.test.ts` (a checker denying c1 → no judgment row, no classifier call, no error row; c2 still judged; the model's standing on c1 carries no `agrees`) — verified to fail with the gate removed. The `agent-findings-judge` reference did NOT move (no call in it is denied; events are not in the compared bytes).
- 2026-09-17 · classifier · review · MINOR `classifierScorer` padded an unscored candidate with `?? 0`: a candidate the provider did not score is now ABSENT from `ranked` (the ranking is the provider's distribution, the `JudgmentRow.probabilities` law). Pinned: `classifierScorer.test.ts` (one row for the scored candidate, none for the other).
- 2026-09-17 · classifier · review · MINOR the `typesafe()` timeout path was untested: `typesafe.test.ts` stubs a fetch that never resolves and rejects only on its own signal's abort, `timeout: 20` → `ClassifierError` 'request failed — timeout', `retryable: false`, no `status`, ONE attempt with `maxRetries: 3`.
- 2026-09-17 · classifier · review · MINOR only the execute-loop judge site was tested: `findings-judge.test.ts` drives the `pauseHere` → `resume` door — zero calls at the pause (the tool threw), exactly one judgment row for the resumed call with the person's answer as the judged `result`, filed before the post-resume model call.
- 2026-09-17 · classifier · review · NOTE `mapResult` defaulted `model: 'unknown'` and `Number(confidence)` could be `NaN`: a reply without a non-empty `model` string and an answer whose `confidence` / `score` / `noul` is missing or not a finite number are refused as `ClassifierError` (`typesafe.ts · numberField`, the unknown-type precedent). Pinned: `typesafe.test.ts` (missing / empty / numeric `model`; missing / string / null `confidence` on a choice).
- 2026-09-17 · classifier · first hosted judge run (Sonnet actor, System One judge, signal cell, RUNS=10, 300 judgments): judge-accuracy 0.18–0.23 vs the model's 0.82–0.96, agrees 0.15–0.24, ≈1,800 tokens / ≈0.4 s per judgment — the broad question fails; next variant = atomic yes/no questions composed in code. Table on the real-model page. SDK removed after the run.

- 2026-09-17 · tool-choice · Part A · `src/core/agent/toolChoice/{types,pick,record}.ts` + README (`Mixed`: Lens for the served list, Trace for the rows). `AgentOptions.toolChoice { classifier; serve?: 'all' | { top }; alwaysServe? }` (the brief named two fields; `alwaysServe` is the third because "any tool the app marks always-served" needs a mark, and a names list is the additive shape), `AgentBuilder.toolChoice` (refuses a second call, a non-Classifier, `top` not an integer ≥ 1, a bad `alwaysServe`; the option form goes through the same door), `Agent` refuses `reactMode: 'classic'` at build (the `.findings()` precedent — the slot runs once there). THREE conflicts with the brief, the code's shape kept each time: (1) the brief's events `toolChoice.picked/outcome/failed` are refused by the registry's own form test (`^agentfootprint\.[a-z_]+\.[a-z_]+$`), so the domain is `tool_choice` — a NEW domain, 25 → 26, 115 → 118 events, the eight doc counts, MENTAL_MODEL's domain line, observability.mdx prose; (2) the Compose stage is SYNC and the brief says "the pick is awaited in buildToolsSlot": the tail after `mergeWire` became one closure `commitWire(served, servedInjections, narrowedTo)` — the unarmed path calls it synchronously so every write lands in the order it always did (the `$setValue(INJECTION_KEYS.TOOLS)` write moved from before the merge to the closure's first line; the merge writes nothing, so no trace row moves — 19/19 references green before the scenario existed), the armed path returns a promise (`void | Promise<void>`, the engine's `StageFunction` shape) and awaits `pickTools` before it; (3) the tools subflow is an ISOLATED runtime and a mount input is frozen, so "recordToolChoice(scope, row) like recordFindings" takes the prior rows as an explicit third argument in the slot (`priorToolChoices`, the `priorIntegrityFindingIds` alias pattern) and the mount's outputMapper carries `toolChoices` back; in the grouped chart the key crosses `sf-llm-call` under the alias in and the base key out, with `dynamicTurnSeed` copying it (the `history` trip). `pick.ts` and `record.ts` are reached through `import()` from the slot AND from `callLLM` (the `judgeLanded` precedent) — neither is a root export; the root exports are the types plus the two leaf constants (`TOOL_CHOICE_QUESTION` = 'tool', `ALWAYS_SERVED_TOOLS` = read_skill · list_skills · skip_step · present, literals pinned to the owners' constants by `narrow.test.ts`). The doors are NOT offered as candidates (asking about a tool served regardless would only dilute the distribution). A call with no candidate beyond the doors makes no classifier call and files no row, so `callLLM` files the outcome only when a pick was attempted (`pickAttemptedFor`). `ranked` is sorted highest first with ties in offered order — the brief said "as given"; a ranking that is not ranked would make "the right tool second" unreadable. `toolsInjections` follows the served set under a narrowing (filtered by a parallel `injectionNames` list — a record's `sourceId` is the owner or the skill, not the name — and re-numbered), so the slot record and the wire agree. `servedTools.current` and `lastServed` are restricted to the served names under a narrowing, so DISPATCH FOLLOWS THE OFFER holds: a narrowed-away name takes the off-wire path.
- 2026-09-17 · tool-choice · Part B · the miss as FOUND: the brief said dispatch "refuses it today (not offered)"; `toolCalls.ts · resolveTool` refuses only a name whose last-served party can no longer answer or a never-served name with two holders — a registry tool off this epoch's wire is answered OFF-WIRE by the party the model last read it under and recorded as `tools.answered_off_wire`. Kept as is: the outcome row carries `miss.wanted`, `tool_choice.outcome` carries `missed`, the missed call RUNS and its result lands, the next call serves the full wire (`after-miss`). Documented in the README and the design page with the honest limit: on a hosted API the wire constrains `tool_use` to the served schemas, so a miss as defined cannot occur there — a wrong ranking shows as a wrong pick or an answer (`firstAgrees` measures it, `miss` cannot), and "a miss costs one extra model call" is the cost on a wire that refuses unserved names, not the measured cost on this one (0 extra calls on the bench). Skips as the brief lists them (`unavailable` also covers a ranking with fewer scored names than N — a top-N over fewer than N scores would drop tools on absent data; `too-few` = fewer than N + 1 candidates; `after-miss` reads the LAST outcome only; `wrap-up` still files the pick row, never narrows — `callLLM` withholds every tool on that call anyway, so the row's `served` is the slot's list while the receipt says `withheld: 'wrap-up'`). `servedView.viewOf` untouched — the committed `dynamicToolSchemas` IS the narrowed list; pinned by `run.test.ts` (every narrowed epoch: `Object.keys(receipt.tools.schemaHashes)` = `view.tools.schemas[].name` = `view.tools.names` = the row's `served` = the request's tools) and by `receipt-conformance.test.ts` § a narrowed tool list (both chart shapes, `clean(r)`).
- 2026-09-17 · tool-choice · Part C · bench `bench/tool-choice.mjs` (`npm run bench:tool-choice`): one skill of eight tools, six scripted steps, the mock model calling `tool_k` at step k WHETHER OR NOT it is served (the only way a miss can occur; stated in the header), three classifier scripts × two conditions + the unarmed twin; cost line first; two laws exit non-zero — the unarmed twin's tools-slot bytes equal `UNARMED_TOOLS_SLOT_BYTES` = 1745 on every call and every advisory row equals the twin's; under top-2 right-first/second never miss, wrong-pair misses on every narrowed tool call with only `after-miss` skips. Print 2026-09-17: unarmed 1745.0 · advisory 1745.0 ×3 (first-agrees 1.000 / 0.000 / 0.000) · top-2 743.0 / 743.0 / 1172.4 (misses 0 / 0 / 3, extra-calls 0, narrowed 7/7 · 7/7 · 4/7); `pick-tokens` `-` and `pick-latency-ms` 0 on the mock — a cost is data only when the provider reported it; `AF_TOOL_CHOICE_CLASSIFIER=typesafe` runs the same table on the hosted classifier (NOT run). Tests: `toolChoice/pick.test.ts` (8), `narrow.test.ts` (11), `record.test.ts` (11), `run.test.ts` (21, the real loop: pick before the call, advisory twin byte-identical requests + keys(on) = keys(off) + `toolChoices`, narrowing agrees across request/receipt/view, the four skips, the miss with `answered_off_wire`, grouped chart, events, refusals), `events/unit/tool-choice-events.test.ts` (6), `receipt-conformance.test.ts` (+2), `registry.test.ts` 115 → 118; byte-identity: the 18 references run first on the wired tree (19/19), copied aside, `agent-tool-choice` generated alone (`-t agent-tool-choice` under `AF_TOOLS_REFERENCE=update`), the 18 `cmp`-equal after; the new reference read back from its bytes: pick · outcome per call, `dynamicToolSchemas` narrowed to two + `read_skill` on calls 1, 2, 4 and the full five on call 3 (`after-miss`), the missed `lookup`'s result in `history`. Docs: toolChoice README, `build/agent.mdx` § Tool choice by classifier (every new root export in prose), scored-choice page § Step 4 + Track, CHANGELOG `## [9.105.0] - 2026-09-17` (package.json version untouched — releases are the owner's), CLAUDE.md capability row, top-level README bullet, the local SKILL.md copy re-synced. Open: the classifier's state is the message + skill id — the conversation tail does not cross the Tools mount; a hosted run of the bench is what decides whether narrowing is ever a default.
- 2026-09-17 · tool-choice · gate · tsc · test:types · build · `bench:tool-choice` (exit 0) · full suite 11379/11400 passed, 20 skipped, ONE red that is an ordering artefact (`release-gate-interlock` read the report the chain regenerated one step later; `docs:truth:report` ran LAST and `test/docs` 75/75 + the interlock alone 6/6 are green after it) · prettier/eslint clean on every touched file (0 errors; the two non-null assertions the packet added were removed) · `docs:truth` no ratchet (+1 documented export) · `cd docs-next && EXPORT=true npm run build && npm run check:site-budget` is RED BY 0.4 KB: `deferred demo async payload: 422.4 KB exceeds 422.0 KB`. First measurement was 422.7 KB; the lazy chunk (`toolChoice/pick.js` + `record.js`, 1.2 KB gzip, chunk 9836) is NOT among the demo's 17 async assets (verified in `.next/react-loadable-manifest.json` — the `import()` law holds), so the growth is on the default graph: the slot's row-building tail, the builder's validation, `Agent.ts` wiring, the two charts' mappers, `callLLM`'s outcome block and the registry strings. Moved the whole armed tail into `toolChoice/compose.ts · composeToolChoice` behind the same `import()` (the slot keeps six lines) — 422.7 → 422.4 KB; what remains is the wiring an armed agent needs to exist at all. Saying so, as the brief asks, instead of raising the ceiling: the owner's call is the ceiling or a trim elsewhere (the budget file's own note names the dynamic-import fix as the next raise, not a fifth number).
- 2026-09-17 · tool-choice · review · MAJOR `narrowServed`'s after-miss skip read the LAST outcome row on the record, not the previous iteration's, so an older miss re-triggered the skip across an intervening iteration that filed no pick (no candidate beyond the doors → no row, `pickAttemptedFor`). Fixed to `outcomeAt(prior, iteration - 1)`: `narrowServed` now takes `iteration` and skips only when the row AT `iteration - 1` exactly carries a `miss`. Pinned: `narrow.test.ts` ("after-miss reads iteration - 1 only" — miss at iteration 1, no row at iteration 2 → iteration 3 narrows; "a miss at exactly iteration - 1 does hold" — miss at iteration 2 → iteration 3 skips).
- 2026-09-17 · tool-choice · review · MINOR `ToolChoiceErrorRow` carried no `narrowedSkipped`, so a `serve: { top }` run whose classifier failed left a reader unable to tell from the error row alone that narrowing was skipped (the README's law implied it could). `compose.ts` now spreads `narrowing.narrowedSkipped` into the error row the same way it already does for the pick row — the reason `narrowServed` computed was being thrown away, not recomputed; absent under `serve: 'all'` (`top` undefined skips the check before `pick.ok` is ever read). `types.ts` doc comment added. Pinned: `run.test.ts` (top-2 with a failing mock classifier → the error row carries `narrowedSkipped: 'unavailable'` and the full wire served; `serve: 'all'` → no such field on the row).
- 2026-09-17 · tool-choice · review · NOTE the toolChoice README's "When the pick runs" section stated the `reactMode: 'classic'` refusal but not the seed's static fallback (`callLLM.ts · registeredToolSchemas`, `scope.dynamicToolSchemas ?? deps.toolSchemas` — a chart with no tools slot): that path runs no Compose stage, so it asks no pick and narrows nothing, same law as the classic-mode case. Sentence added beside it.
- 2026-09-17 · tool-choice · head-to-head on the first host (8 questions × 3 conditions, real model + real classifier): first-pick agreement 6/23; top-2 narrowing cut tools-slot bytes 57% (2.24 MB → 0.96 MB) with 8 misses recorded, every answer the same verdict; ≈300 ms and ≈900 tokens per pick. Table on the scored-choice page. The chatbot's switch left OFF.


- 2026-09-17 · ontology · Part A · `src/ontology/{types,define,instruction,serve,index}.ts` + README (`Mixed`: Map for the shapes and `defineOntology`, Lens for `ontologyPiece` and the ask), door `src/doors/ontology.ts` + `package.json` exports/typesVersions (the classify door's pattern; the SEVENTEENTH door — `documented-doors`, `door-aliases`, `subpath-exports` and CLAUDE.md's entry-points line updated). `defineOntology` refuses by name (identifier-safe ids, dangling edge, unknown source, empty/over-long text ≤ 512, repeated alias/via/holding/edge, `ONTOLOGY_LIMITS` counts), detaches and deep-freezes, `configured` absent stays absent; `ontologyHash` = SHA-256 over the receipt's `stableJson` of the five declared fields (key-order independent; edge order IS part of the identity — the served `relations:` keep declaration order, so the hash and the piece agree). THREE places the brief and the code differ, the code's shape kept: (1) the brief says the `via` check lives in `AgentBuilder.build`; the registry (`registryByName`) is composed in `Agent.buildChart` from the constructor, so the check sits there beside `buildToolRegistry` — still thrown by `.build()`, message `ontology '<id>' names tool '<name>' that is not registered (node …, source …)`; a provider-served tool cannot be named (met only at dispatch), recorded in the README. (2) The brief threads `ontology: true` to seed and callLLM; seed needs the map to WRITE it, so seed gets the frozen `Ontology` (`SeedStageDeps.ontology`) and callLLM gets the flag (`CallLLMStageDeps.ontology: true`) and reads the committed key — one record, read by the wire and the rebuild alike. (3) The brief counts 20 byte-identity references; the tree holds 19 (`ls test/core/tools/reference` before this packet) — 19 untouched, `agent-ontology` the 20th. Also: the grouped chart crosses the key into `sf-llm-call` under `AgentChartDeps.hasOntology` AND value-conditionally (the `findingsLedger` line, plus the deps gate); the ask is registered as an always-on instruction (`ONTOLOGY_INSTRUCTION_ID` = 'ontology', the `outputSchema()`/`.findings()` pattern the brief's reading list points at), NOT appended to the piece; `AgentBuilder.ontology` refuses a bare spec (a non-frozen object without `hash`) so the definition stays the one validator.
- 2026-09-17 · ontology · Part B · `ontologyPiece(spec)` pure: constant header quoting `CONTEXT_FIELD_MEANINGS.domainDefinitions/limitations/evidenceRefs`, identity line `ontology: <id> · version: <v>`, then `nodes:` / `sources:` / `held by:` / `relations:` / `known, not held here:` exactly as the brief spells the lines; nodes and sources SORTED by id (so key order moves neither the hash nor the bytes), edges in declaration order; 64 lines per section `+K more (cap 64)`, the id line 64 ids `+K more`; author text folded onto one line (a `\n\nrelations:\n` inside a meaning cannot forge a section — pinned). `ContextSource` += `'ontology'`. `callLLM`: ONE gated read of `scope.ontology` under `deps.ontology === true`, `import('../../../ontology/serve.js')` (the `judgeLanded` precedent), the event, and the join injections → recovery → ontology → findings (`systemPieces` only); `servedView.viewOf` mirrors it through `readRunConstant(location, 'ontology')` + `ontologyOf` narrowing. The bare `serve.ts` citation in callLLM's findings comment became `findings/serve.ts` (the citations test refuses an ambiguous basename once `ontology/serve.ts` exists). `ONTOLOGY_INSTRUCTION` (4 lines, "Ontology v1.") passes `unprovable` at the strictest lifetime — no `modelFacingScan` ledger entry needed (census unchanged: 95 files / 183 literals).
- 2026-09-17 · ontology · Part C · event `agentfootprint.ontology.served` — a NEW domain `ontology` (26 → 27, 118 → 119; the eight doc counts, MENTAL_MODEL's domain line, observability.mdx prose, `DomainWildcard` += `agentfootprint.ontology.*`, an `EmitBridge` attached only under the arm — the findings/tool_choice wiring). Tests: `test/ontology/define.test.ts` (14), `test/ontology/serve.test.ts` (13 — incl. "every non-label token is a string of the declaration"), `test/core/agent/ontology.test.ts` (22 — BOTH chart shapes: the key once on seed's commit, the piece on every epoch byte-equal wire = rebuild, order after recovery/before findings with a real `.findings()` standing, the receipt law, the event per call, the registry refusal + a skill tool + `read_skill` passing, the builder door incl. the option form, keys(on) = keys(off) + `ontology` with identical messages/tools and the armed prompt = the unarmed prompt + ask + piece), `test/events/unit/ontology-events.test.ts` (5), `receipt-conformance.test.ts` +2 (both shapes, `clean(r)`), `registry.test.ts` 118 → 119, `silent-success` REFUSES_A_SECOND_CALL += `ontology`, `modelFacingSurfaces` +2 PRODUCERS rows (the instruction at ALWAYS_ON_INSTRUCTION, the piece at LEDGER_PIECE with every line shape as a marker); byte-identity: the 19 run first on the wired tree (20/20 with the harness), copied aside, `agent-ontology` generated alone (`-t agent-ontology` under `AF_TOOLS_REFERENCE=update`), the 19 `cmp`-equal after; the new reference read back: `ontology` on `commitLog[0].overwrite` (one write; the normaliser prints its 64-hex `hash` as `<digest>` — a fingerprint, not a run-minted value, but the same shape), pieces `[base, instructions, ontology]` on both epochs, the served text ending in `known, not held here: port_error_rate`. Docs: `src/ontology/README.md`, context-contract README § "The ontology is the data behind three of these fields", `build/agent.mdx` § "The ontology — a map, not a door" (every new door export in prose), `docs/design/2026-09-ontology.md` (the ruling, the laws, the bench table — measured on the first host, not here), CHANGELOG `## [9.106.0] - 2026-09-17` (package.json version untouched — releases are the owner's), CLAUDE.md capability row + module-map row + entry-points line, README bullet, doors README.
- 2026-09-17 · ontology · gate · tsc · test:types · build · full suite 11441/11462 passed, 20 skipped, ONE red that is the ordering artefact (`release-gate-interlock` read the report before `docs:truth:report` ran LAST; `test/docs` 76/76 green after it) · prettier clean and eslint 0 errors on every touched file (the packet's own non-null assertions removed; the 8 warnings left are pre-existing lines) · `docs:truth` no ratchet (`agentfootprint/ontology` 15/15 documented; the event documented, never observed — no example runs it) · `cd docs-next && EXPORT=true npm run build && npm run check:site-budget` GREEN: deferred demo **424.4 KB** gzip across 17 async assets (ceiling 431 KB; was 422.4 KB on 9.105.0, +2.0 KB — 6.6 KB headroom left). Read from the export: the piece's header text sits in the demo's shared async chunk (7339), because `servedView.ts` imports `ontology/serve.ts` STATICALLY (the rebuild is synchronous, the findings precedent) and servedView is on the lens's graph — so callLLM's `import()` of the same module lands in the parent chunk rather than its own (the budget file's own `sideEffects` note names this shape). The rest of the growth is the wiring an armed agent needs to exist at all: the builder door + validation, `Agent.buildChart`'s registry check, seed's write, the boundary line, the registry strings, `ONTOLOGY_INSTRUCTION`. The docs-next prebuild regenerated `docs-next/content/docs/api/**` (34 generated pages, line-number shifts only) — the owner's `docs(api): regenerate` commit, left in the tree.
- 2026-09-17 · ontology · review · MAJOR `defineOntology` silently stripped an unknown key on a node, a source, a node-source entry or an edge, so `ontologyHash(rawSpec)` could differ from `defineOntology(rawSpec).hash` whenever the raw spec carried a stray key. Fixed: `checkNoUnknownKeys` refuses by name at each of the four shapes (`node '<id>' carries unknown key '<key>'.`, `source '<id>' carries unknown key '<key>'.`, `nodes['<id>'].sources[<i>] carries unknown key '<key>'.`, `edges[<i>] carries unknown key '<key>'.`) before any field is read; `ontologyHash`'s doc now states it is defined over a VALIDATED spec. Also refused (cheap, the review's note): a node id that collides with a source id (`node id '<id>' collides with source id '<id>'.`) and a node alias that collides with another node's id (`node '<id>' alias '<alias>' collides with node id '<alias>'.`). Pinned by `test/ontology/define.test.ts`: an unknown key on each of the four shapes refuses by name; the two collisions refuse by name; `ontologyHash(spec) === defineOntology(spec).hash` for a clean spec (pre-existing assertion, still green).
- 2026-09-17 · ontology · review · MINOR `test/ontology/serve.test.ts`'s "every non-label token … is a string of the declaration" checked `JSON.stringify(SPEC).includes(token)` — a substring test, so a leaked library word that happened to sit inside a JSON key (or any substring of any declared string) would pass unnoticed. Rewrote as a whole-word check: a `CHECK_SPEC` fixture whose words share nothing with the header prose or the section labels, tokenized on the piece's own separators (` · `, `←`, `—`, `→`, `(`, `)`, `:`, `,`) and whitespace; the label vocabulary (the header's own words — read live off the served piece, since the header is pinned constant — plus the section headings, `via`, `aliases`, `coverage`, `configured`, `yes`, `no`, `version`, `ontology`, `more`, `cap`) subtracted, and every remaining token asserted to be a whole word of some declared value. Added a negative case planting `smuggled-by-the-library` into a copy of the body text and asserting the check throws on it, so the check is shown to bite.
