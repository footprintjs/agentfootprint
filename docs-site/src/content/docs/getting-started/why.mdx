---
title: Why agentfootprint?
description: What makes agentfootprint different from other agent frameworks.
---

import CodeFile from '@/components/CodeFile.astro';

> Your support agent told a customer their refund was processed. Six weeks later they come back and ask *"why did you tell me my refund was processed when it wasn't?"* You go to look. The agent is gone. Logs are scattered across three services. The decision evidence is not there. **This is the gap agentfootprint exists to close.**

## The problem

Most agent frameworks give you execution. You get an answer, but you can't *explain how the agent got there*. Logs are disconnected. Traces are spans without meaning. When the LLM hallucinates, you find out from your users.

## Connected evidence

agentfootprint gives you **connected evidence** — grounded, auditable, LLM-readable. Every iteration of the ReAct loop, every tool call with its args + result, every context injection, every decision branch is captured as a typed event during one DFS traversal — no instrumentation, no post-processing.

The agent + tool registration is built declaratively. The framework owns the loop, so it can record everything that happens inside it:

<CodeFile path="examples/core/02-agent-with-tools.ts" region="build" title="examples/core/02-agent-with-tools.ts (region: build)" />

And because the framework owns the loop, observability is just attaching listeners — no SDK, no agent.observe wrapper, no separate tracing pipeline:

<CodeFile path="examples/core/02-agent-with-tools.ts" region="observe" title="examples/core/02-agent-with-tools.ts (region: observe)" />

Every event is typed. Every payload is what you'd want — `toolName` + `args` on tool_start, `result` on tool_end, `iterationCount` + token totals on turn_end. No string-parsing logs. No "I think this means the tool was called."

## $0 test runs — scripted ReAct

Tool-using agents are notoriously hard to test because the LLM's behavior is non-deterministic. `mock({ replies })` solves that — script the LLM's decisions turn-by-turn, run the agent against the script, get a deterministic, free, instant test:

<CodeFile path="examples/features/07-mock-multi-turn-replies.ts" region="scripted-replies" title="examples/features/07-mock-multi-turn-replies.ts (region: scripted-replies)" />

Iteration 1 calls the tool with the scripted args; iteration 2 returns the final answer. The tool actually executes; the agent loop is real. Only the LLM is mocked. Swap `mock(...)` for `anthropic(...)` to ship — the rest of the agent is identical.

## No graph DSL required

Composition is just chained `.create()` builders, not a separate graph language. Two agents in series? `Sequence`. Multiple critics with merge? `Parallel`. Iterate until quality bar? `Loop`. The compositions are how multi-agent systems are built — there's no `MultiAgentSystem` class, no `Orchestrator` to learn:

<CodeFile path="examples/patterns/02-reflection.ts" region="reflexion-recipe" title="examples/patterns/02-reflection.ts (region: reflexion-recipe — Reflexion as a 30-line recipe)" />

That's Reflexion (Shinn 2023), expressed as `Sequence(Agent, critique-LLM, Agent)`. Tree-of-Thoughts is `Parallel(Agent × N) + LLM-rank`. Debate is `Loop(Agent × 2 + judge)`. Every named pattern in the agent literature is a composition of the same 2 primitives + 4 compositions. **You learn the substrate; the field's growth lands as recipes on top.**

## Built on footprintjs

agentfootprint is built on [footprintjs](https://footprintjs.github.io/footPrint/) — a flowchart-pattern execution engine for backend code. You don't need to learn footprintjs to use agentfootprint; it's a substrate that does the bookkeeping.

**What footprintjs provides (you get for free):**
- The execution engine that runs the agent loop as a flowchart
- The narrative recorder system that captures what happened during execution
- Time-travel checkpoints + cross-server replay
- The flowchart visualization in the [agent playground](https://footprintjs.github.io/agent-playground/)

**What agentfootprint adds on top:**
- The 5-layer taxonomy: 2 primitives (LLM, Agent), 4 compositions (Sequence, Parallel, Conditional, Loop), named patterns as recipes, context-engineering Injection primitive, infrastructure features
- LLM provider adapters (Anthropic, OpenAI, Bedrock, Ollama)
- The `Injection` primitive (Skill / Steering / Instruction / Fact / Memory / RAG)
- Memory subsystem (4 types × 7 strategies, including Causal — decision-evidence replay)
- MCP integration, resilience decorators, mocks-first development surface

The same way `autograd`'s forward-pass traversal is what makes gradient inspection automatic, footprintjs's flowchart traversal is what makes agentfootprint's typed-event stream and replayable traces automatic. The framework owns the loop — that's why it can show you everything inside it.

## Next steps

- [Quick Start](/agentfootprint/getting-started/quick-start/) — build your first agent
- [Key Concepts](/agentfootprint/getting-started/key-concepts/) — the 5-layer taxonomy
