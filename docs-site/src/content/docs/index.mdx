---
title: agentfootprint
description: Context engineering, abstracted. The substrate for agents whose every decision, tool call, memory write, and context injection is a typed event you can replay six months later.
template: splash
hero:
  tagline: "Context engineering, abstracted. Every decision an Agent makes — tool call, memory write, context injection — is a typed event you can replay, audit, and explain. Six months later. On a different machine."
  actions:
    - text: 5-min Quick Start
      link: /agentfootprint/getting-started/quick-start/
      icon: right-arrow
      variant: primary
    - text: Why agentfootprint?
      link: /agentfootprint/getting-started/why/
      icon: document
      variant: secondary
    - text: Compare to other frameworks
      link: /agentfootprint/getting-started/vs/
      icon: external
      variant: minimal
---

import CodeFile from '@/components/CodeFile.astro';
import { Card, CardGrid } from '@astrojs/starlight/components';

## The one thing that's different

Every other agent framework ships agents you debug with **logs**. agentfootprint ships agents whose every decision, tool call, memory write, and context injection is **captured as a typed event during one DFS traversal** — no instrumentation, no post-processing.

The framework owns the loop, so the framework can record everything that happens inside it. Same way `autograd`'s forward-pass traversal makes gradient inspection automatic, agentfootprint's flowchart traversal makes the typed-event stream + replayable traces automatic.

## In 30 seconds — runs offline, no API key

<CodeFile path="examples/memory/01-window-strategy.ts" region="define" title="examples/memory/01-window-strategy.ts (region: define)" />

```bash
npm install agentfootprint footprintjs
npm run example examples/memory/01-window-strategy.ts
```

Outputs *"You just asked about your previous message."* in under 100ms. Deterministic, free, no API key. Swap `mock(...)` for `anthropic(...)` / `openai(...)` / `bedrock(...)` / `ollama(...)` for production. Nothing else changes.

## Pick your starting door

<CardGrid stagger>
  <Card title="🎓 New to agents" icon="rocket">
    Start with the [5-min Quick Start](/agentfootprint/getting-started/quick-start/). First agent runs offline against `mock()`. No API key needed.
  </Card>
  <Card title="🛠️ LangChain / CrewAI / LangGraph user" icon="seti:typescript">
    Start with [vs other frameworks](/agentfootprint/getting-started/vs/) — honest comparison, migration sketch.
  </Card>
  <Card title="🏗️ Architecting an enterprise rollout" icon="setting">
    Start with [Deployment](/agentfootprint/guides/deployment/) — multi-tenant identity, audit trails, peer-deps, observability stack.
  </Card>
  <Card title="🔬 Researcher / extending the framework" icon="open-book">
    Start with [Skills, explained](/agentfootprint/guides/skills-explained/) — context-engineering essay; the strongest writing in the docs.
  </Card>
</CardGrid>

## What you can build

Three example shapes, all runnable end-to-end with `npm run example examples/<file>.ts`:

- **[Customer support agent](/agentfootprint/guides/skills-explained/)** with skills, memory, and an audit trail — `examples/context-engineering/06-mixed-flavors.ts`
- **[Research pipeline](/agentfootprint/getting-started/key-concepts/#patterns--named-compositions)** with multi-agent fan-out + LLM merge — `examples/patterns/05-tot.ts` (Tree-of-Thoughts) or `01-self-consistency.ts`
- **[Streaming chat agent](/agentfootprint/guides/streaming/)** with token-by-token output to a browser — see streaming guide for the SSE pattern

## The two differentiators

### 1. Causal memory — replay *why*, not just *what*

`defineMemory({ type: CAUSAL })` records the agent's decision evidence — every `decide()` value the flowchart captured. New questions cosine-match past queries; matching snapshots inject the prior decision evidence; the LLM answers from EXACT past facts, not reconstruction. See [Memory guide § Causal](/agentfootprint/guides/memory/#causal-memory--replay-decisions-not-just-messages).

### 2. Trace export → trace replay

Every run exports as one JSON checkpoint. Persist it (Redis, Postgres, S3); resume on a different process, day, or server. The same JSON shape feeds SFT / DPO / process-RL training pipelines (`exportForTraining({ format })` on the v2.5 roadmap).

## Mocks first, prod second

| Boundary | Dev (mock) | Prod (one-line swap) |
|---|---|---|
| LLM provider | `mock({ replies })` | `anthropic()` · `openai()` · `bedrock()` · `ollama()` |
| Embedder | `mockEmbedder()` | OpenAI / Cohere / Bedrock embedder (planned v2.6) |
| Memory store | `InMemoryStore` | `RedisStore` · `AgentCoreStore` (planned: pgvector / Pinecone) |
| MCP server | `mockMcpClient({ tools })` | `mcpClient({ transport })` |
| Tool execute | inline closure | real implementation |

Ship the patterns first; pay for tokens last.

## What's next

- [Why agentfootprint?](/agentfootprint/getting-started/why/) — the substrate argument with worked examples
- [Quick Start](/agentfootprint/getting-started/quick-start/) — first agent in 5 minutes
- [Key Concepts](/agentfootprint/getting-started/key-concepts/) — the 5-layer taxonomy
