**Fold** — the context-error family: one question ("which context piece made
this wrong?") and the pluggable answers to it.

## What it reads / what it writes
- Reads a recorded failing run.
- Writes nothing into it.

## The one law here
One question, many finders, each declaring what its answer is worth.

## Files
- `finders/` — the finders themselves.
